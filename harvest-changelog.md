## [2.0.0-build.T40R] — BUILD PHASE — 2026-07-20
### T-40R — WP05 gate remediation: wired reachable messaging entry points + real-factory gate coverage + dead-scaffold deletion (re-QC CONFIRM-MERGE 9.4, zero critical)
- Remediates the WP05 gate's decisive FAIL (6.0/10, do-not-unlock): all 10 CFE/opt-out/quiet-hours/deliverability critical conditions were individually sound and every send-gate was mutation-proof, but the WRITE/TRIGGER side of T-39's messaging services (`SequenceService`, `EmailSendService`, `ObjectionService`, `ThreeWayHandoffService`, `EdificationService`) had zero reachable caller — no API route, no UI affordance, no cron — so a rep could not actually enroll a sequence, fire a cadence, use the objection tool, or trigger a 3-way handoff. This unit is wiring-only: the six mutation-proven gate files (`send-decision.ts`, `SendComplianceGate`, deliverability, and the org-gating seams) are untouched — confirmed byte-identical against trunk both at initial merge and at re-QC.
- New reachable entry points, every one session-gated, ownership-checked, and `x-user-id`-inert: `POST /api/messaging/sequence` (enroll) plus an hourly Inngest cron (`messaging-scheduled-sequence-run`, idempotent `runDueSteps`) and a `SequenceEnrollPanel` rep affordance; `POST /api/messaging/objection` (list/prepare, producing a PENDING/null-CFE draft that the existing send-decision gate refuses to send unclearanced) plus an `ObjectionCoachPanel`; `POST /api/messaging/handoff/trigger` (wires `EdificationService.generate`, org-gated) and `POST /api/messaging/handoff/join` (upline-id boundary enforced) plus an hourly `returnIfLapsed` cron and a `BridgeUplinePanel`; `POST /api/messaging/email-send` (routes through the existing CFE-cleared + `SendComplianceGate` + `isChannelDeliverable` chain, org-domain-verified, fails safe to HELD when the send credential is missing). All four surfaces funnel through one shared, lazily-constructed factory (`src/services/messaging/send/production-wiring.ts`) rather than each hand-wiring its own gate calls — a single seam to keep gated instead of four.
- Dead pre-WP05 scaffold deleted: `engine.service.ts`, `template.service.ts`, `handoff.service.ts`, `cadence.service.ts`, `api-routes.ts`, and the orphaned `messaging.test.ts` that exercised them — ungated, unreachable code that inflated the test count without covering anything a rep could hit. Deletion is grep-confirmed dead first and guarded by a resurrection test.
- Two QC rounds folded into this landed unit. The initial re-QC (7.2/10, REJECT, zero critical) found the wiring itself solid — all four surfaces reachable and gated, dead scaffold gone, all 9 gate/service files byte-identical, clean build — but caught a test-net hole of the same class as an earlier remediation (T-R17): the new shared `production-wiring.ts` factory had zero direct test coverage, and the judge's own mutation (forcing `buildEmailSendService` to report `deliverable: true` unconditionally) left all 1821 tests green, meaning a future gate-weakening in the factory would ship undetected. The fix added `production-wiring.test.ts` (16 tests) importing the REAL `buildEmailSendService`/`buildPlatformSmsSendService`/`buildSequenceDispatcher`/`buildSequenceService` factories directly (not hand-built equivalents) and asserting opted-out/non-deliverable/non-CFE-cleared paths are HELD through the real factory, with a mutation-teeth proof (forcing `deliverable: true` in the factory flips the non-deliverable test red; reverting restores green). Fixing this also surfaced a latent DI gap — `SendComplianceGate` was never threaded through the factory's `db` param, always resolving to the global singleton — closed with a minimal, disclosed, non-behavior-changing `ProductionWiringOverrides.sendGate` optional second argument (defaults to `{}`; every production call site still passes 0-1 args, so the seam is unexploitable in production and unchanged from trunk).
- Non-blocking, deferred to remediation backlog: `handoff/join` has an API surface but no dedicated UI affordance yet (T-R22); Spanish/i18n for the new surfaces stays P1-deferred per §0.6 precedence (T-R21, already tracked from the T-40 gate run).
- Branched from 0cda2de (pre-T-R4); merged clean against trunk's T-R4 (DB-level audit immutability/anchoring/`AuditCheckpoint`) — fully disjoint lanes (T-40R: messaging routes/crons/panels + production-wiring + dead-scaffold deletion touching 24 files under `src/app/api/messaging`, `src/app/community`, `src/services/messaging`, and `tests/unit`; T-R4: `src/services/compliance/audit/*` + `prisma/schema.prisma`'s `AuditCheckpoint` model), zero file overlap, zero conflicts; T-40R carries no schema change of its own. `prisma generate` re-run after merge (picks up trunk's `AuditCheckpoint` model) and before typecheck. 1849 tests post-merge (net +54 over trunk's 1795: T-40R's own wiring/reachability and production-wiring factory coverage, less the deleted dead-scaffold suite — no regression); typecheck 0 errors; build green; all 4 guards (middleware, api-auth, contrast, no-opacity-on-text) pass; the four messaging routes (`/api/messaging/sequence`, `/api/messaging/objection`, `/api/messaging/handoff/trigger`+`/join`, `/api/messaging/email-send`) and `/community/[contactId]` all confirmed present in the build's route manifest. Lands the final open item under the WP05 priority spine and unblocks the WP05 gate re-run.

## [2.0.0-build.T-R4] — BUILD PHASE — 2026-07-20
### T-R4 — DB-level AuditEntry immutability + anchoring/tail-truncation detection + deep-frozen returned rows (QC 9.4, zero critical) — §16 COMPLIANCE TRUST-SPINE HARDENING
- Layers a second, independent enforcement level under the WP11 audit trust spine (master-spec §16.1 "immutability (append-only, signed audit)") on top of T-10's existing app-level guard (no `update`/`delete` method exists on `AuditRepository`/`PrismaAuditRepository`/`InMemoryAuditRepository` in the first place). New hand-authored migration `20260720140000_t_r4_audit_hardening` adds a `prevent_audit_mutation()` Postgres trigger function that `RAISE EXCEPTION`s (a loud, real thrown error — not a silent `DO INSTEAD NOTHING` rule, which would violate §17.7's "never silently no-op" doctrine for evidence surfaces) on any UPDATE or DELETE against `AuditEntry`; INSERT (append) is untouched. A trigger was chosen over a bare `REVOKE` because a revoke depends on knowing the exact DB role the app connects as (varies by deploy target) and can be bypassed by a later, more-privileged grant — the trigger applies regardless of which role issues the statement.
- New `AuditCheckpoint` model (additive, same migration) closes the hash chain's one honest gap documented in `hash-chain.ts`: a chain alone cannot detect truncation of its own TAIL (deleting the most-recently-appended rows, with nothing appended after to show a broken `prev_hash` link). New `src/services/compliance/audit/anchoring.ts` periodically anchors the current chain head (`sequence`/`head_entry_hash` from `getChainTail()`) plus a row count into this checkpoint table, SHA-256-hashed (`checkpoint_hash`) over the anchored fields. `AuditCheckpoint` gets the identical `prevent_audit_mutation()` trigger — a checkpoint that could itself be edited or deleted would defeat the tail-truncation detection it exists to provide. `AuditService.verifyAnchoring()` (new, on `AuditService`, wired via an optional constructor-injected `checkpointRepository` — every existing single-argument `new AuditService(repository)` call site keeps working unchanged) re-checks the store against the LATEST checkpoint on record and flags a mismatch as tail truncation; it also refuses to accept a newer checkpoint with a lower `entry_count` than a prior one, so an attacker cannot defeat detection by inserting a new checkpoint matching their own truncated tail.
- Every `AuditEntryRecord` returned by `query`/`getById` — from either repository, in-memory or Prisma-backed — is now deep-frozen (new exported `deepFreeze()` in `audit-service.ts`, replacing the prior shallow `Object.freeze`), so a consumer can no longer mutate a nested field (e.g. `classifier_data`) in place either.
- 12 new tests in `tests/unit/audit-hardening.test.ts` covering the DB trigger's reject-UPDATE/reject-DELETE-while-allowing-INSERT behavior, checkpoint creation/hash computation, tail-truncation detection (a shortened chain fails `verifyAnchoring` against its last-known checkpoint), the checkpoint-count-can-only-grow guard, and deep-freeze reaching nested fields on both repositories' returned rows.
- Purely additive: no existing column, table, enum value, or method signature altered or removed; the two new optional `AuditService` constructor/method surfaces (`checkpointRepository`, `createCheckpoint`/`verifyAnchoring`) are opt-in and throw a clear, named error rather than silently no-op'ing when a checkpoint repository isn't wired in yet, matching the repo's established not-yet-wired-in-dependency convention (e.g. `activity-ledger.ts`'s `DownlineScopeResolver`).
- Branched from 0cda2de (trunk's T-R5 head); merged clean, zero conflicts — this unit's diff (`prisma/schema.prisma`, the new migration, `anchoring.ts`, `audit-service.ts`, `compliance/audit/index.ts`, `compliance/index.ts`, one new test file) touches no file any concurrently-landing lane also touches. `prisma generate` re-run after merge (new `AuditCheckpoint` model) and before typecheck. 1795 tests post-merge (net +12 over trunk's 1783 — this unit's own coverage; no regression); typecheck 0 errors; build green; all 4 guards (middleware, api-auth, contrast, no-opacity-on-text) pass.

## [2.0.0-build.T-R5] — BUILD PHASE — 2026-07-20
### T-R5 — Postgres-backed shared rate-limit/session-activity/login-history store for multi-instance deploy (QC 9.2, zero critical) — MULTI-INSTANCE LAUNCH-GATE PREREQ
- Closes a launch-gate remediation item (§16.4/§18.10): T-12's rate-limiter, session-activity ("last seen"), and login-history/credential-stuffing stores (`src/services/security/rate-limiter.ts`, `src/lib/auth/session-security.ts`, `src/services/security/credential-stuffing.ts`) were process-local (`InMemory*Store`) — correct on a single instance, but on a multi-instance/serverless (Vercel) deploy each instance keeps its own counters, so a distributed attacker (or just ordinary multi-instance routing) can multiply their effective attempt budget by the instance count. This unit swaps all three stores for a Postgres-backed, cluster-wide implementation of the same already-swap-ready store interfaces — additive only, same caps/windows/lockout math as before; this is a store swap, not a policy change.
- Three new additive-only models (migration `20260720130000_t_r5_shared_store`): `RateLimitCounter` (key-scoped counters with an optimistic-concurrency `version` token — `PostgresRateLimitStore.put()` does a conditional `UPDATE ... WHERE key = ? AND version = ?`, so two instances racing to increment the same key never silently lose one instance's attempt; the losing writer gets a `RateLimitConflictError` and `RateLimiter.check()` retries against a fresh read rather than overwriting a stale count — a brand-new key instead relies on the table's own primary-key constraint to turn a racing double-insert into a conflict); `SessionActivityRecord` (plain last-write-wins upsert, keyed by `sessionActivityKey(userId, boundAt)` — no optimistic concurrency needed since `touch()` only ever records "now", never a computed delta); `LoginHistoryRecord` (append-only, INSERT-only feed for credential-stuffing anomaly scoring — atomic by construction, no read-then-write race to guard against).
- All three Postgres-backed stores fail closed on any store error (DB unreachable, query failure) rather than silently degrading to "allow" — a store outage denies the request rather than open the gate wide, per the repo's existing fail-closed doctrine (mirrors T-R6's GDPR-clock fail-safe pattern). Exact pre-existing rate-limit semantics (caps, windows, lockout thresholds) are preserved bit-for-bit; only the storage backend changed.
- 16 new tests in `tests/unit/shared-store.test.ts`, including "PROVE" cases with teeth: cross-instance cap enforcement (two simulated instances sharing one Postgres-backed counter correctly hit the same cap neither could reach alone), atomic increment under concurrency (racing writers via the `version` CAS never both succeed), fail-closed on store error (a simulated DB outage denies rather than allows), exact rate-limit semantics preserved (caps/windows/lockout unchanged from the in-memory implementation), and shared session-activity/login-history persistence round-trips. A separate suite confirms default store wiring safely defaults to in-memory in the test env and never touches Postgres unless explicitly configured.
- Branched from 174bf50 (pre-T-39); merged clean against trunk's T-39 (messaging + `Message.approved_by`/`approved_at` + `OutreachSequence`/`OutreachSequenceStep`/`ThreeWayHandoff`) — fully disjoint lanes (T-R5: security/auth stores + 3 new schema models; T-39: messaging + its own 3 new schema models), the two units' only shared file was `prisma/schema.prisma`, on disjoint model additions, both intact post-merge; `prisma generate` re-run after merge (stale pre-merge client) and before typecheck. 1783 tests post-merge (net +16 over trunk's 1767 — this unit's own store coverage; no regression); typecheck 0 errors; build green; all 4 guards (middleware, api-auth, contrast, no-opacity-on-text) pass.

## [2.0.0-build.T39] — BUILD PHASE — 2026-07-20
### T-39 — WP05 messaging assembly: outreach sequences + edification/3-way handoff + objection tree + email + reachable conversation UI (re-QC CONFIRM-MERGE 9.5, zero critical) — FINAL WP05 BUILD UNIT
- Closes out WP05 (§10.2, §10.5-10.7, uiux §5.7/§4.7) by assembling the remaining messaging surfaces on top of the send-gating backbone T-37/T-38 already landed: **outreach sequences** (`sequence.service.ts` + `sequence-cadence.ts`) that pre-schedule steps and re-gate each one individually at send time — the sequence engine holds no message client of its own, so every actual send is routed through T-37's existing seam and structurally cannot bypass CFE-clearance/approval/compliance gating; a sequence pauses outright on a contact reply, opt-out, or block rather than continuing to fire steps into a conversation that has moved on, and defers (not skips) a step that lands inside quiet hours. **Edification / three-way handoff** (`edification.service.ts`, `three-way-handoff.service.ts`) is org-gated and doctrine-clean, with its scripted `callScript` fail-closed against §10.6's 200-300 word floor. **Objection tree** (`objection-tree.ts` + `objection.service.ts`, 8 objections) produces PENDING/null-CFE-outcome drafts only — `send-decision.ts`'s existing gate refuses anything not independently CFE-cleared, so the tree cannot manufacture a sendable message on its own. **Email path** (`email-send.service.ts` + `email-send-client.ts`) mirrors the platform-SMS 3-gate chain (CFE-cleared-and-approved → `SendComplianceGate` → deliverability) and fails safe (HELD, not sent, not fabricated) when `RESEND_API_KEY` is unset. **Conversation UI**: a new reachable `/community/[contactId]` page + `GET /api/contacts/[contactId]/conversation` route render `ConversationTimeline`, folding in the agent-sent `AgentSentBadge` (honest send-attribution — "sent by your agent · approved by you [date]", sourced from the new `Message.approved_by`/`approved_at` denormalized off `DraftMessage` at send time) and a durable `cfe_audit_id` link from each sent `Message` to its `AuditEntry` (closing the T-37 QC audit-trail gap) — reachable via a new Link on `ContactCard`, session-gated, ownership-checked (`findFirst` scoped to `{ id, user_id }`, 404s rather than leaking a cross-rep contact), `x-user-id` inert, decrypted through the existing T-22 Vault path.
- Additive-only schema (migration `20260720130000_t39_outreach_sequences`): `Message.approved_by`/`approved_at` (nullable, denormalized send-time approval attribution) plus three new models — `OutreachSequence`, `OutreachSequenceStep`, `ThreeWayHandoff` (the latter's `joined_at`/`returned_at` explicitly `@db.Timestamptz(6)`, a QC fix — see below). Merged clean against trunk's T-R17 `ComplianceConsent` `@@unique([contact_id, consent_type, version])`: disjoint models, both intact post-merge, `prisma generate` re-run after merge (stale pre-merge client from `npm ci`'s postinstall) and before typecheck.
- Two build/QC rounds folded into this landed unit. Initial QC (8.0/10, do-not-merge, zero critical) found the send-gating backbone itself mutation-proof — all 6 gates (sequence-bypass, objection-uncleared, email-gate, missing-key, cross-org leak, pause-on-reply) load-bearing under mutation, no send-bypass on any blocked case — but flagged 3 MUST-FIX gaps: (1) `ConversationTimeline`/`AgentSentBadge`/`ThreeWayHandoffCard` built but mounted on no route at all, making 10 uiux §5.7/§4.7 ACs unreachable by a rep (the same shape as an earlier WP04 Today-CTA finding); (2) `edification`'s `callScript` at 174 words, under the §10.6 200-300 floor, with its own test asserting character count rather than word count (a false guard); (3) `ThreeWayHandoff.joined_at`/`returned_at` missing `@db.Timestamptz(6)`. All three fixed without touching any of the 6 gate files or the 3 UI components (byte-identical diffs, verified) — new `/community/[contactId]` page mounts the timeline + badge, reachable from `ContactCard`; `callScript` rewritten to 283/287 words with a test that now asserts word count with teeth; the handoff model's two timestamp columns got `@db.Timestamptz(6)` in an amended migration. Re-QC (CONFIRM-MERGE 9.5/10, zero critical) independently re-ran the word count and re-dropped `user_id` from the ownership query (red, as expected) before confirming.
- 1767 tests post-merge (net +98 over trunk's 1669: 9 new test files — `sequence.service.test.ts`, `email-send.service.test.ts`, `conversation-timeline.service.test.ts`, `three-way-handoff.service.test.ts`, `objection.service.test.ts`, `edification.service.test.ts`, `conversation-timeline.test.ts`, `conversation-route.test.ts`, `conversation-mount.test.ts` — this unit's own coverage; no regression). Clean merge, zero conflicts (including `prisma/schema.prisma`); typecheck 0 errors; build green; all 4 guards (middleware, api-auth, contrast, no-opacity-on-text) pass; `/community/[contactId]` confirmed present in the build's route manifest. This is the final WP05 build unit — closes the WP05 priority spine.

## [2.0.0-build.T-R11] — BUILD PHASE — 2026-07-20
### T-R11 — offline local-persistence + idempotent reconnect-sync for the warm-market ritual, Layers 1-2 (QC 9.3, zero critical)
- Closes the master-spec §5.4 AC-5.4-7 gap ("each layer resumes independently with no data loss") for `WarmMarketRitual.tsx`: the T-26 engine's `getState()` only reports which layer is COMPLETE, with no read endpoint for an in-progress Layer 1/2 seed roster, so a reload — online or offline — used to lose whatever the rep had typed but not yet submitted. New `RitualDraftSnapshot` (`src/app/ritual/warm-market/offline.ts`) persists Layer 1's entries, Layer 2's selected clusters/assignments, and the current layer client-side; on mount the component now prefers a present local draft over the last server-confirmed layer, so an optimistically-advanced-but-still-in-flight layer survives a reload instead of rolling back.
- New codebase-wide, framework-free offline primitives added under `src/lib/offline/`: `storage.ts` (`KeyValueStorage` abstraction — lazily probes real `window.localStorage`, degrading to an in-memory `MemoryStorage` under SSR, in this repo's node-env Jest suite, or when a privacy-mode browser throws on `setItem`; `readJson`/`writeJson`/`removeStoredItem` never throw — corrupt data or a full quota degrades to "session state intact, only cross-reload persistence lost," per §5.4 "Error") and `online-status.ts` (`isOnline()` / `subscribeOnlineStatus` wired to real `navigator.onLine` and `online`/`offline` window events, defaulting to "online" when `navigator` is unavailable rather than spuriously blocking).
- New `PersistentOfflineQueue` (`src/lib/offline/offline-queue.ts`) generalizes the in-memory `OfflineActionQueue` T-34 built for the Shift into a persisted, idempotent-replay FIFO queue any feature can reuse: every enqueued mutation is written to storage immediately and removed only after its handler resolves, so a reload or crash mid-replay leaves exactly the not-yet-applied tail queued — never a re-applied or dropped mutation. `createRitualQueueHandlers` wires the ritual's two mutation kinds (Blank Canvas, Qualities Flip) to replay through the SAME `/api/harvest-method/*` endpoints the online path already uses, with the load-bearing check that a 2xx HTTP response is not treated as "applied": both handlers inspect `result.ok` and throw on `false`, so a server-side soft-gate rejection or layer-order violation on replay is a genuine failure — the mutation stays queued, nothing is falsely marked synced, and the compliance/order gates apply identically on replay as they do online (no bypass).
- `BackgroundMatchingLayer` (Layer 3) degrades honestly when offline (§5.4 "Layer 3's matching requires connection"): the submit button is replaced by a deferred notice rather than a fabricated or silently-disabled control, with tile capture still fully rendered; the online path is unchanged (no regression). `WarmMarketRitual.tsx` renders an offline banner the instant `navigator.onLine` is false at mount, mirroring the pattern `ShiftView.tsx` already established.
- 86 new tests across two new files: `tests/unit/offline-primitive.test.ts` (17 cases covering `storage.ts`/`online-status.ts`/`PersistentOfflineQueue` — persisted round-trip, FIFO idempotent replay, crash-mid-replay recovery, stop-on-first-failure ordering, mid-replay-enqueue deferral) and `tests/unit/warm-market-offline.test.ts` (69 cases, including "TEETH" proofs that a previously-saved draft actually hydrates the live component on render — not just that the snapshot round-trips in isolation — and that queued replay genuinely hits the real server-side soft-gate/layer-order checks rather than bypassing them).
- Tight, disjoint diff (`WarmMarketRitual.tsx`, `BackgroundMatchingLayer.tsx`, `ritual.module.css`, new `offline.ts` + `src/lib/offline/*`, 2 test files) — no schema change, no route touched, no overlap with trunk's T-37 (messaging)/T-R14 (scheduler)/T-R17 (consent ordering), branched from 39236e3 and merged clean against dd83762. 1669 tests post-merge (net +86 over trunk's 1583 — this unit's own coverage; no regression; baseline re-verified directly against pre-merge trunk, not assumed from the changelog).

## [2.0.0-build.T-R17] — BUILD PHASE — 2026-07-20
### T-R17 — deterministic consent-current ordering + per-contact unique on ComplianceConsent (re-QC 9.4, zero critical)
- Remediates the LOW/non-critical finding deferred at T-38 QC (§9.2): `MessagingConsentLedger`'s "current version" reads (`captureConsent`, `hasMessagingConsent`, `getConsentHistory`) used a bare `findFirst`/`findMany` `orderBy: { version: 'desc' }` with no tiebreak, and nothing at the DB level stopped two concurrent `captureConsent` calls for the same contact from both reading the same "current" version and both inserting the same next version number — two rows silently sharing one version, with undefined DB row order deciding which one "wins" as current.
- New exported `CURRENT_CONSENT_ORDER_BY` (`src/services/compliance/messaging-consent/messaging-consent-ledger.ts`) — `version desc, timestamp desc, id desc` — makes "current" deterministic even when two rows do share a version: `timestamp` breaks a version tie in favor of the row actually written later, `id` is the final, always-unique absolute tiebreak for two rows inserted in the same instant. All three read paths now consume this single constant rather than each hand-rolling its own `orderBy`.
- New `prisma/schema.prisma` `@@unique([contact_id, consent_type, version])` on `ComplianceConsent` (migration `20260720100000_t_r17_consent_version_unique`) turns the race from silently-allowed into DB-detectable: the loser's `create` throws Prisma `P2002`, which `captureConsent` now catches and retries (bounded, `MAX_VERSION_RACE_ATTEMPTS = 5`) against the now-current version rather than surfacing a duplicate. Deliberately a plain `@@unique`, not a partial index: Postgres treats `NULL` as distinct from every other `NULL` in a unique index by default, so a plain unique on `(contact_id, consent_type, version)` never constrains WP11's onboarding-consent rows (`contact_id` unset → `NULL`, `version` always the default `1`) against each other — those keep coexisting unconstrained, exactly as before. The constraint only ever fires on a real, matching, non-null `contact_id` — i.e., precisely the per-contact messaging-consent shape this unit is fixing.
- QC fix folded in before merge: the initial pass's determinism test asserted the ordering constant's *shape* but drove its fake Prisma client from a hardcoded ordering rather than the `orderBy` value `captureConsent`/`hasMessagingConsent`/`getConsentHistory` actually pass — so the test would keep passing even if a caller regressed back to a bare `{ version: 'desc' }`. Re-QC fix makes the fake sort by whatever `args.orderBy` it's actually given and asserts each call site passes `CURRENT_CONSENT_ORDER_BY` by reference, so the test now discriminates a real regression instead of only checking the constant's own value.
- Additive-only migration, no other model touched; merged clean against trunk's T-37 (`DraftMessage.send_hold_reason`, migration `20260720120000_t37_send_hold_reason`) — the two units' only shared file was `prisma/schema.prisma`, on disjoint models, both additions intact post-merge. 1583 tests post-merge (net +11 over T-37+T-R14's 1572 — this unit's own ordering/race/unique-constraint coverage; no regression).

## [2.0.0-build.T37] — BUILD PHASE — 2026-07-20
### T-37 + T-R14 — two disjoint-lane new-feature units, merged sequentially (QC 9.4 / QC 9.4, zero critical)
- **T-37** (QC 9.4) — the WP05 spine: the two honestly-engineered SMS send paths (§10.1). A new pure, I/O-free `resolveDraftClearance` (`src/services/messaging/send/send-decision.ts`) is the single shared invariant both paths are built on — "neither path may dispatch content that is not CFE-cleared AND human-approved" — consuming the `cfe_outcome`/`approval_state`/`edited_after_approval` state WP04's Approval Inbox already produced, never re-running the CFE engine itself. **First-touch composer handoff** (`first-touch-composer.service.ts`) turns a cleared, approved draft into a one-tap `sms:{E.164}?body=…` deep link the native Messages composer opens — a real blue bubble from the rep's own number, never claimed as "sent" until the rep's own one-tap `confirmHandoff` — subject to opt-out/quiet-hours but deliberately NOT platform/TCPA/deliverability-gated (the rep's own number is never Twilio-provisioned). **Platform SMS send** (`platform-sms-send.service.ts`) is the automated Twilio/A2P 10DLC path and the most heavily gated surface in WP05: a message dispatches only when all three gates pass in order — CFE-cleared-and-approved, `SendComplianceGate` (opt-out + recipient quiet hours + TCPA consent, T-38), and `isChannelDeliverable` (A2P brand+campaign approved + platform number assigned, T-36) — any failure, or a missing `TWILIO_*` key, holds (never crashes, never fabricates delivery) and records why on the new `DraftMessage.send_hold_reason` column. New additive, nullable `send_hold_reason` (migration `20260720120000_t37_send_hold_reason`) captures one of 11 hold reasons (`OPTED_OUT`/`QUIET_HOURS`/`NO_TCPA_CONSENT`/`NOT_DELIVERABLE`/`NOT_CFE_CLEARED`/`NOT_APPROVED`/`EDITED_AFTER_APPROVAL`/`CHANNEL_MISMATCH`/`TWILIO_UNCONFIGURED`/`NO_PLATFORM_NUMBER`/`NO_PHONE`/`ERROR`) — the "held for review, nothing was lost" evidence the rep-facing timeline surfaces; it never causes a send by itself. New `/api/messaging/compose-handoff` (+ `/confirm`) and `/api/messaging/platform-send` routes. 53 new tests across 5 files (`send-decision.test.ts`, `first-touch-composer.service.test.ts`, `platform-sms-send.service.test.ts`, `twilio-messaging-client.test.ts`, `tests/unit/messaging-send-routes.test.ts`), including the load-bearing proof that neutering any one of the platform path's three gates alone lets content leak the other two would have stopped.
- **T-R14** (QC 9.4) — the first autonomous (cron-triggered) agent dispatch trigger. Before this unit the only paths onto the durable queue were the session-gated user-invoked `POST /api/agents/dispatch` and a single event-triggered Inngest function — no autonomous trigger existed at all, contradicting the product's "24/7 / while you slept" premise. New `runScheduledDispatch` (`src/services/agent-runtime/scheduled-dispatch.ts`) is pure, package-free handler logic wired to an hourly (`0 * * * *`) Inngest cron in `inngest-functions.ts`; it CONSUMES rather than forks the existing pipeline — every due unit of work is enqueued through the same `DurableQueue.send()` boundary the user-invoked path already uses, so every existing guard (idempotency, per-contact do-not-contact/agents_paused, the CFE fail-closed gate) applies unchanged downstream. Guards respected twice: the scheduler consults the same T-31 `RunGate` once per rep before enqueueing anything (a killed/over-budget rep is skipped outright, no event reaches the queue), and `AgentRuntime.runAgent` re-checks it again downstream. Idempotency reuses the existing `IdempotencyLog` table (read-only `wasAlreadyDispatched`, per-UTC-day key) so an hourly tick is a liveness/catch-up mechanism, not the cadence itself — a rep/contact/agent combination fires at most once per UTC day regardless of tick frequency, and a pass skipped one hour is naturally retried the next. Recipient-timezone quiet hours and the CFE stay correctly out of scope (both are unmodified, downstream, still fully in effect — this scheduler only produces CFE-gated drafts sitting in the Approval Inbox, it sends nothing itself). 16 new tests in `tests/unit/scheduled-dispatch.test.ts`. No schema change.
- Fully disjoint lanes (T-37: messaging/send + additive schema; T-R14: agent-runtime/inngest, no schema) — both merged clean, no conflicts, including in `prisma/schema.prisma` where T-37's `send_hold_reason` is the only change either unit makes. 1572 tests post-merge (net +69 over T-R6's 1503: 53 new T-37 + 16 new T-R14; no regression).

## [2.0.0-build.T-R6] — BUILD PHASE — 2026-07-20
### T-R6 — guard computeGdprClock against unparseable clockStartedAt, fail-safe to max-urgent (QC 9.6, zero critical)
- Closes a defensive gap in the GDPR Art. 33 / master-spec §16.7 item 4 72-hour notification clock (`computeGdprClock`, `src/services/security/incident/gdpr-clock.ts`): a clock-applicable incident whose `clockStartedAt` was truthy but unparseable (a corrupt/garbage timestamp string) fell through into `new Date(input.clockStartedAt).getTime()` → `NaN`, then NaN arithmetic — `referenceMs > deadlineMs` (i.e. `x > NaN`) is always `false`, so `overDeadline` would silently read `false`/"plenty of time" on a broken input, exactly the wrong direction for a regulatory deadline; `new Date(deadlineMs).toISOString()` on a NaN `deadlineMs` would additionally throw `RangeError: Invalid time value`.
- New `Number.isNaN(startMs)` guard, checked immediately after the parse and before any deadline/elapsed/remaining arithmetic runs, routes an unparseable `clockStartedAt` to the same maximally-urgent fail-safe shape the pre-existing null/undefined "not started" branch already returns (`applicable: true`, `status: 'OPEN'`, `overDeadline: true`, `approachingDeadline: true`, `deadline`/`elapsedMs`/`remainingMs` all `null`) — reached via its own distinct branch, so the two fail-safe paths (missing vs. unparseable) stay independently testable and neither can silently swallow the other.
- 3 new cases in `tests/unit/incident-gdpr-clock.test.ts`: the unparseable-timestamp case fails safe (max-urgent, no thrown `RangeError`); a valid `clockStartedAt` is unaffected by the new guard (normal 72h arithmetic still applies); and the pre-existing null-`clockStartedAt` case still takes its own distinct fail-safe branch rather than being absorbed into the new NaN check.
- Tight 2-file diff (`gdpr-clock.ts` + its test), disjoint from every other unit on trunk — no schema, route, or other service touched. 1503 tests post-merge (net +3 over T-R15+T-R10's 1500 — this unit's own NaN-guard coverage; no regression).

## [2.0.0-build.T-R15-R10] — BUILD PHASE — 2026-07-20
### T-R15 + T-R10 — two disjoint-lane remediation units, merged sequentially (re-QC 9.4 / QC 9.3, zero critical)
- **T-R15** (re-QC 9.4) — narrowed the master-spec §0.5 "selling / closing (as extraction)" forbidden-vocab rule in `VocabularyClassifier` (`src/services/compliance/vocabulary.ts`) to require an explicit extraction-object cue instead of matching bare `sell`/`close(d)`/`closing`. The rule was first added at T-R15's initial pass (commit `0a105de`) to close a real §0.5 gap — CFE stage-1 had no rule at all for that row — but its first cut over-blocked: ordinary, non-extraction uses of "closed the deal" ("closed the deal on her new apartment/car/the merger"), accounting/furniture idioms ("hard close of the books", "soft-close drawers"), and the legitimate "close rate" §9.7 Field Trainer's Ratio metric all tripped it. The fix (commit `23a5b88`) requires either (1) a person-noun directly as the verb's object (`close them`/`closing the prospect`), (2) `close(d)/closing the sale/deal` followed by `with`/`on` + that same person-noun, or (3) the `sales closer`/`sale closing` idiom — and drops the ungated bare `hard/soft close` alternative entirely, since regex can't reliably separate its sales-technique sense from its accounting/furniture senses. New `tests/unit/vocabulary-classifier.test.ts` (39 cases) covers both the still-blocked extraction phrasing and the now-clean ordinary-English cases, plus asserts `FORBIDDEN_TERMS` holds exactly 14 rules (12 pre-existing + 2 new: selling, closing).
- **T-R10** (QC 9.3) — `GET /api/contacts/pipeline` (and the `/community` page it feeds) used to return four hardcoded illustrative contacts (an `_meta` flag marked the payload as demo data) regardless of which rep called it, so every rep's Community home showed the same fake four, and T-28's real per-contact flag toggle 404'd against those fake ids on the live page. The route now calls `PipelineService.getPipelineSummary(userId)` (T-23's real, session-scoped, `where: { user_id: userId }`-only, decrypted contact read) instead of the `demoContacts()` stub, which is deleted outright. `/community/page.tsx` and its new `community.module.css` were updated to render the real per-stage summary shape (including each contact's actual persisted `is_recruit_target`/`is_client` flags, not an assumed-false default) in place of the demo fixture's shape. New `tests/unit/pipeline-route.test.ts` (7 cases, incl. forged `x-user-id` inert / 403 when not onboarding-gated / real seeded contact renders / zero-contact rep gets a clean empty response) and `tests/unit/pipeline-route-import-safety.test.ts` (2 cases), plus 2 new cases appended to `tests/unit/warm-market.test.ts` proving the real isRecruitTarget/isClient flag passthrough and that `getPipelineSummary` never leaks one rep's contacts to another.
- Both units are disjoint-lane (T-R15 touches only `vocabulary.ts` + its test; T-R10 touches only the pipeline route/service, the community page + its stylesheet, and their tests) and disjoint from each other and from everything else on trunk. Both merged clean, no conflicts — including in the one file both this pair and an already-landed unit (T-R8) touch, `tests/unit/warm-market.test.ts`: T-R8's edits sit at the top of the file (imports, `importContacts` retirement block) and T-R10's new tests are appended after the existing "never the raw ciphertext envelope" test, so the 3-way merge resolved without overlap. 1500 tests post-merge (net +50 over T-R1's 1450: 39 new vocabulary-classifier + 7 new pipeline-route + 2 new pipeline-route-import-safety + 2 net-new warm-market cases; no regression).

## [2.0.0-build.T-R1] — BUILD PHASE — 2026-07-20
### T-R1 — purge stray non-Claude provider refs from committed docs/config (§0.3 Claude-only doctrine, QC 9.4, zero critical)
- Closes a documentation-only drift from §0.3 ("no substitution permitted, no fallback outside the Claude roster"): `README_DEPLOYMENT.md`'s live-integrations env-var list named `OPENAI_API_KEY` — a stray reference to a provider this build never calls — where it should have named the actual credential the Claude-only agent layer reads. Corrected to `ANTHROPIC_API_KEY` (Claude-only AI agent layer — Haiku 4.5 / Sonnet 5 / Opus 4.8; see `.env.example`).
- `harvest-handoff.md`'s "Model Routing Summary" and orchestration-config sections carried a pre-build-phase, pre-pivot roster (deepseek/kimi/glm/gemini/minimax) left over from the PRD-authoring phase, plus a "User-Shared Information" note recording a since-superseded GPT model swap. All three are replaced in place with a superseded-notice pointing back at this entry and at master-spec §0.3, rather than deleted outright — the notice records that a non-Claude roster existed pre-pivot and was PRD-authoring-phase-only, never a build-phase fallback path, so no build-phase behavior changes.
- Scope is deliberately narrow: only the two committed docs named above. The frozen pre-pivot PRD-authoring corpus (`harvest-prd.md`, `harvest-qc-checklist.md`, `harvest-todo.md`, `_foundation-draft.md`, `inbox/`, `wp-specs/`) is untouched by design — those are historical artifacts of a phase that predates §0.3, rewriting them would falsify the historical record rather than correct live guidance, and a full editorial pass over that corpus is deferred to T-R18. `.env.example` and every runtime/config source file were already Claude-only (verified at T-30/T-31 — `AnthropicRuntimeClient`'s fail-closed `claude-*`-only construction, `CLAUDE_MODEL_IDS`); this unit is docs-only, no source/schema/test touched.
- Tight 2-file diff (`README_DEPLOYMENT.md`, `harvest-handoff.md`), no schema change, no route touched. 1450 tests post-merge (unchanged from T-R9 — docs-only unit, no test surface).

## [2.0.0-build.T-R9] — BUILD PHASE — 2026-07-20
### T-R9 — DSAR self-service export decrypts User PII + excludes password_hash/MFA secrets via deny-by-default allowlist (QC 9.1, zero critical)
- Closes a real DSAR export defect (§16.3 "the export must contain the data subject's actual data" + §16.4/§0.4 "no secret ever leaves via a self-service channel"): `DataRightsService.processExport` used to serialize the raw `User` row with no field selection at all — `solution_number`/`anchor_statement` (AES-256-GCM ciphertext envelopes at rest, §3.2) went out unintelligible, and `password_hash`/`mfa_methods` (bcrypt hash + encrypted TOTP secret/recovery-code hashes) went out as secret material, both unexamined.
- New `buildUserExportRecord` decrypts `solution_number`/`anchor_statement` via each field's own name-only key accessor (`getSolutionNumberEncryptionKey`/`getWhySessionEncryptionKey`, imported by name only — no dependency on either unit's business logic) using the same independently-degrading per-field pattern as the existing Contact-PII export path: a corrupt/mismatched-key field degrades to `DSAR_FIELD_DECRYPTION_UNAVAILABLE`, never a crash and never the raw ciphertext string.
- New explicit `USER_EXPORT_ALLOWED_FIELDS` — a deny-by-default ALLOWLIST, not a denylist of the two known-bad fields: `password_hash` and `mfa_methods` are excluded by construction (never read off the row at all), so a future `User` column added to the schema is excluded by default until someone deliberately adds it here having considered whether it belongs in a legal disclosure. `mfa_enrolled` (a bare boolean, no secret payload) stays in; `emailVerified`/`image` (dead Auth.js-adapter scaffold fields, always null under this app's Credentials+JWT flow) are left off.
- Adds `WHY_SESSION_ENCRYPTION_KEY` documentation to `.env.example` (the Seven Whys anchor-statement encryption key this export path also decrypts through). Additive-only, no schema change; tight 4-file diff (`data-rights.ts` export path, `.env.example`, `tests/jest.setup.ts`, `tests/unit/data-rights.test.ts`). 1450 tests post-merge (net +3 over T-R12/T-R8's 1447 — this unit's own decrypt/allowlist-exclusion coverage; no regression).

## [2.0.0-build.T-R12-R8] — BUILD PHASE — 2026-07-20
### T-R12 + T-R8 — two disjoint-lane remediation units, merged sequentially (QC 8.9 / QC 9.6, zero critical)
- **T-R12** (QC 8.9) — Mission Control Action Queue classifier hardened, defense-in-depth: `draftToItem` (`src/services/mission-control/zones/action-queue.ts`) now flags `review_flagged` on ANY non-`PASS` `cfe_outcome` (`d.cfe_outcome !== 'PASS'`), not just the literal `'FLAG'` value, in addition to the pre-existing `approval_state === 'HELD'` check. Today `BLOCK` always implies `HELD` by construction (`agent-runtime.ts`'s `bandToOutcome`/`held` derivation) and `actOnQueueDraft` is separately fail-closed on `cfe_outcome !== 'PASS'` regardless of what this classifier decides, so the widening changes no live behavior — it exists so this UI classifier can never surface a one-tap Approve affordance for a non-PASS draft even if that invariant ever drifted. 3 new boundary tests in `tests/unit/mission-control-today-service.test.ts`.
- **T-R8** (QC 9.6) — retired the dead `ContactService.importContacts` (`src/services/warm-market/contact.service.ts`) plus its now-orphaned `normalize`/`splitName` helpers. This method predated T-22's AES-256 encryption of `Contact.phone`/`.email`; once those columns became per-call-IV ciphertext, its dedup lookup (an equality match on the caller-supplied plaintext phone/email) could never match a ciphertext column again — dead code kept alive only by its own unit test, flagged at T-22 QC and tracked as T-R8. The one live import path, `VaultService.importBatch` (`./vault/vault.service.ts`), already dedupes correctly via keyed `phone_hash`/`email_hash`. `tests/unit/warm-market.test.ts` swaps the 4 dead-path exercise tests for a retirement-proof block (grep-clean: no source file references `importContacts`/`normalize`/`splitName`).
- Both units are disjoint-lane (T-R12 touches only `action-queue.ts` + its test; T-R8 touches only `contact.service.ts` + its test) and disjoint from each other and from everything else on trunk; both merged clean, no conflicts. 1447 tests post-merge (net +3 over T-38's 1444 — T-R12's own boundary coverage; T-R8 nets +0, swapping 4 dead-path tests for 4 retirement tests — no regression).

## [2.0.0-build.T38] — BUILD PHASE — 2026-07-20
### T-38 — WP05 send-compliance foundation: global opt-out + recipient-timezone quiet hours + TCPA consent ledger + unified SendComplianceGate (QC 9.2, zero critical)
- New general-purpose `OptOutRegistryService` (master-spec §10.4/§3.4/§16.2) generalizes the existing per-reason opt-out fan-out convention (previously only exercised by the minor-flow in `vault.service.ts`) to every reason (`stop_reply`/`manual`/`wrong_person`/`minor`) over the trunk `OptOutRegistry` table. Keyed by `identifier_hash`, not `user_id`/`contact_id`, so one write is immediately visible to every rep's copy of the same recipient — global, permanent (no revoke method exposed), and cross-rep by construction, well under the §10.4/AC-10.9-4 60-second propagation bar. `isOptedOut` treats any read failure as opted-out — never as clear.
- New recipient-timezone quiet hours (§10.4: "distinct from the rep's own notification quiet hours"): pure, dependency-free `quiet-hours.ts` resolves the recipient-local minute-of-day via `Intl.DateTimeFormat`'s built-in IANA/DST resolution (no third-party tz library) and blocks sends outside the 8 AM-9 PM local window. Fail-closed by construction: a missing, empty, or unrecognized timezone id resolves to "cannot determine -> assume within quiet hours" (blocked), never defaulted to UTC or the rep's own zone. New additive `Contact.timezone` (nullable, deliberately not backfilled/defaulted).
- New per-recipient TCPA consent ledger (`MessagingConsentLedger`, §10.4/§16.2/§16.3): reuses WP11's existing `ComplianceConsent` table rather than forking a parallel model, additively extended with nullable `contact_id` + `version` (default 1) + a `(contact_id, consent_type)` index — a row with `contact_id` set is a per-recipient TCPA record; a row with it null is the original per-platform-user record, so pre-T-38 rows are untouched. `hasMessagingConsent` returns true on exactly one path (a confirmed latest-by-version record with `given: true`); no record, a revoked record, or any read failure all resolve to false.
- New unified `SendComplianceGate.evaluate(contact, channel, now)` (§10.4, qc-checklist WP05 checkpoints 4-5): the single seam T-37's send paths and T-39's sequence engine must consult before any automated dispatch, composing global opt-out -> recipient-timezone quiet hours -> TCPA consent (for `SMS_PLATFORM` only — the rep's own human-confirmed `SMS_HANDOFF` composer send is out of this consent record's scope by design) in order, deny-by-default: every individual sub-check is already fail-closed, and the whole composition is additionally wrapped so any unexpected throw (including a missing identifier for the channel being sent on) resolves to `{ allowed: false, reason: 'ERROR' }`. There is exactly one path to `{ allowed: true }`.
- New inbound STOP-keyword capture webhook (`POST /api/compliance/opt-out/inbound`, §10.4/§10.9-4): a machine-to-machine route (no rep session exists on a carrier webhook) authenticated by a shared secret (`INBOUND_SMS_WEBHOOK_SECRET`, read by name only, compared via `crypto.timingSafeEqual` with a length-mismatch short-circuit before the constant-time compare so an unequal-length secret can't 500 into a length-probing oracle) — fails closed (rejects every request) when unconfigured, rather than accepting everything. Explicitly does not read/trust any `x-user-*`/`x-auth-*` header, so `verify-api-auth.mjs`'s forged-identity guard is moot here by construction (no identity to forge). Alongside it, `GET/POST /api/compliance/messaging-consent` and `GET/POST /api/compliance/opt-out` give reps session-gated manual read/write access to the same two ledgers.
- QC fixes folded in before merge (QC 9.2, zero critical): added `compliance-routes-auth.test.ts` (forged `x-user-id`/`x-organization-id` inert across all three rep-facing compliance routes) and fixed a real Next.js App-Router build break (a non-route named export, `INBOUND_WEBHOOK_SECRET_ENV_VAR`, had to move out of `route.ts` into the service module — App Router rejects non-route exports from a route file). One LOW/non-critical finding deferred to backlog as T-R17 (a `findFirst(orderBy:{version:'desc'})` without a secondary tiebreak / no DB-level unique on `(contact_id, consent_type, version)` — a theoretical concurrent-write race on one contact, not a systematic allow-bias; tracked, not blocking).
- Additive-only schema (`Contact.timezone` + `ComplianceConsent.contact_id`/`.version`/index, migration `20260719100000_t38_optout_quiet_tcpa`) alongside T-36's 5 deliverability tables/4 enums — the two units' only shared touched file was `prisma/schema.prisma`, which merged cleanly with both units' additions intact. 1444 tests post-merge (net +68 over T-36's 1376 — this unit's own opt-out/quiet-hours/consent-ledger/send-gate/route-auth coverage; no regression).

## [2.0.0-build.T36] — BUILD PHASE — 2026-07-20
### T-36 — WP05 deliverability provisioning: A2P 10DLC + email warm-up + fail-closed channel-deliverability gate (QC 9.3, zero critical)
- New A2P 10DLC lifecycle (master-spec §10.3): `A2PProvisioningService` drives brand registration and campaign registration through the spec-verbatim `UNREGISTERED -> PENDING -> APPROVED/REJECTED` state machine (`a2p-state-machine.ts`, pure transition logic only — SUBMIT/APPROVE/REJECT/RESUBMIT, no illegal transition reachable), backed by new additive `A2PBrandRegistration`/`A2PCampaignRegistration`/`PlatformPhoneNumber` tables + `A2PProvisioningStatus`/`PlatformNumberStatus` enums (migration `20260719090000_t36_deliverability_provisioning`). A platform phone number is provisioned only after its campaign is APPROVED and is tracked through `UNPROVISIONED -> PROVISIONED -> ASSIGNED -> RELEASED`.
- New email domain authentication + sender warm-up (§10.3): `EmailDeliverabilityService` checks SPF/DKIM/DMARC (`EmailAuthRecordStatus`: `NOT_CONFIGURED`/`PENDING`/`VERIFIED`/`FAILED`) and drives a per-domain warm-up plan (`EmailWarmupStage`: `NOT_STARTED`/`RAMPING`/`WARMED`/`PAUSED`) with a ramping daily-volume cap, backed by new additive `EmailDomainAuthentication`/`EmailWarmupPlan` tables. SPF/DMARC are real, credential-free public DNS TXT lookups (`node:dns/promises`); DKIM needs a provider-specific selector (`EMAIL_DKIM_SELECTOR`, read by name only) and resolves to `NOT_CONFIGURED` — never a fabricated `VERIFIED` — when unset.
- New fail-closed `isChannelDeliverable` gate (`gate.ts`) is the single seam T-37's send paths (or any future caller) must consult before any platform-sent message: `SMS_PLATFORM` and `EMAIL` both resolve to `deliverable: false` on a missing sending domain, an unrecognized channel, or any unexpected error from either underlying service — this gate never lets a crashed check pass as a green light. One explicit, spec-documented exception (§10.1): `FIRST_TOUCH_COMPOSER` (first-touch SMS via composer handoff, sent from the rep's own number via a native `sms:` deep link, never through Twilio/A2P) always reports deliverable, because platform provisioning is simply not that path's concern — callers must not conflate it with `SMS_PLATFORM`.
- New SC5 admin status surface: `GET /api/admin/deliverability` (ADMIN-only via `withRole`, same verified-session pattern as `/api/agents/kill-switch` — no forged `x-user-id`/`x-organization-id` header is ever read) surfaces read-only A2P brand/campaign/number status plus email domain auth/warm-up status per organization, so ops can confirm "deliverability is provisioned, not discovered" before launch rather than finding out in production.
- Mockable Twilio/email clients, fail-safe on missing credentials: `createTwilioClient()` (`twilio-client.ts`) reads `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN` by name, lazily, per-request, and returns `null` — never throws — when either is unset; every caller treats a `null` client as Twilio UNCONFIGURED and resolves to a not-yet-sendable posture (`twilioConfigured: false` on the readiness summary), never a crash and never a fabricated APPROVED/sendable result. No `twilio` SDK dependency added — Twilio's REST surface is called via the runtime's built-in `fetch` through this narrow DI-mockable interface only. Same posture on the email side: `DnsEmailAuthClient` has no credentials to be missing at all (SPF/DMARC are public DNS).
- Full deliverability audit trail (`deliverability-audit.ts`, mirrors the existing compliance audit-emit contract): every brand/campaign transition, number provision/assign/release, and warm-up start/advance/pause emits a typed `DeliverabilityAuditEvent`. Additive-only across the board — no existing model, route, or CFE/runtime path touched; routes construct every service lazily in-handler (same build-safe, key-less-build convention as the rest of the codebase). 1376 tests post-merge (net +40 over T-35R's 1336 — this unit's own A2P/email-warmup/gate/admin-route coverage; no regression).

## [2.0.0-build.T35R] — BUILD PHASE — 2026-07-19
### T-35R — WP04 gate remediation: wire Today primary CTA to /shift ritual entry (verified, zero critical)
- Closes the WP04 gate's blocking boundary-between-units finding (T-35 gate run, checkpoint 9 unreachable via named entry): `/today`'s primary CTA ("Start today's 30 minutes") was hardcoded `disabled` and wired to nothing — a rep could not reach the daily ritual via any shipped UI, leaving master-spec §9.8 ("Entered from Today's primary CTA") and uiux §5.2 AC-5.2-2 functionally unmet despite T-34's `/shift` ritual being complete and live.
- `src/app/today/page.tsx`'s CTA is now a real `<Link href="/shift">`, matching the repo's existing styled-Link pattern; the stale T-32 QC deferral comment (justifying `disabled` while `/shift` didn't exist) is replaced with the T-35R rationale. `/shift` is itself a gated downstream page (`GATED_DOWNSTREAM_PAGE_PREFIXES`), so an authenticated-but-not-onboarded rep following the CTA still lands in onboarding first, not directly in the ritual.
- Zero compliance consequence — this was a UI reachability gap only, not a fail-open/fail-closed defect. New source-level teeth in `tests/unit/mission-control-ui.test.ts` (renderToStaticMarkup can't reach the CTA's client-side `'ready'` state, so assertions target the CTA's own JSX directly, not a vacuous whole-file match): asserts the CTA links to `/shift`, is not `disabled`/`aria-disabled`, renders the expected label, that `/shift` is gated downstream, and that `src/app/shift/page.tsx` actually exists.
- Additive-only, no schema/runtime/CFE change; tight 2-file diff (`src/app/today/page.tsx` + `tests/unit/mission-control-ui.test.ts`). 1336 tests post-merge (net +6 over T-34's 1330 — this unit's own CTA-wiring coverage; no regression).

## [2.0.0-build.T34] — BUILD PHASE — 2026-07-19
### T-34 — WP04 The Shift + the two ratios (20:5:1 learning state) (re-QC 9.0, zero critical) — fifth and final WP04 companion
- New Learning State surface (master-spec §9.7-§9.8, uiux §5.3): `GET /api/learning-state` computes the Agent's Ratio (introductions → responses → appointments set → confirmed shows, the "20" funnel) and the Field Trainer's Ratio (the "5:1" close funnel) as pure, deterministic functions over `Contact.pipeline_stage`/linked `Appointment` data in `services/learning-state/ratios.ts` — no Prisma import in that module, so the funnel math is exhaustively fixture-testable and can never silently drift with how a caller happens to query the DB. Both ratios are SHOWN, always paired with an explainer (never a bare, unexplained number), rendered as `RatioCard`s on `/today` and in the Shift's Open phase.
- New The Shift ritual (`/shift`, backed by `GET/POST /api/shift`, `/api/shift/begin`, `/api/shift/action`, `/api/shift/close`): the Open → Work → Close → Done learning-state phase machine (`ShiftView.tsx` + per-phase components). Open recaps the briefing (3 lines max) + one motivational line + surfaces a grace-day offer automatically when yesterday broke the streak and a grace day is still available. Work is a one-card-at-a-time focus stack (AC-5.3-1) — approve-with-inline-edit / respond-to-flagged / confirm-appointment / log-introduction / mark-attendance cards — with a plain mm:ss-up timer that structurally never renders an alarm/overtime state (AC-5.3-2: no conditional styling keyed off elapsed time at all). Skip moves a card to the end of the stack once; twice-skipped cards leave the Shift server-side (`ShiftService.buildCandidateStack`). Close shows the recap (approvals/confirmations/logs) and advances the streak.
- New additive schema: `LearningState` (per-user ratio/streak/status cache) and `ShiftSession` (one session per rep per `session_date`, phase/mode/stack/elapsed/streak/grace-day state) — migration `20260718180000_t34_learning_state_and_shift`. Fully additive alongside T-31's `AgentKillSwitch` and T-33's `DraftMessage.decline_reason`/`decline_note`; no existing model touched.
- Fail-closed flagged-draft approve in the Shift (QC fix D2, mirrors T-32's Mission Control fail-closed-queue-approve fix): a Work-phase card backed by a `DraftMessage` whose CFE outcome is not a clean `PASS` never gets a one-tap Approve — only Decline plus a plain `<a href="/inbox">` deep-link into T-33's real Approval Inbox, with the compliance band (Blocked-by-review vs. caution) shown on the card itself. Enforced in two layers: `WorkPhase.tsx`'s `isNonPassDraft` gate (checked against the literal `cfeOutcome` value, not just the card's derived `type`, as defense against a stale/mismatched card) hides the button client-side, and `ShiftService.actionCard` / `POST /api/shift/action` independently refuse the mutation server-side regardless of what the client renders (`ShiftApprovalRequiresReviewError`) — the UI gate is convenience only, not the enforcement boundary.
- QC re-pass fixes (base pass → re-QC 9.0, zero critical): (1) literal-threshold test coverage added for the ratio bands' boundary values (exact 20/5/1 threshold inputs, not just above/below fixtures) so the "20:5:1" labels can't silently drift off their defining numbers; (2) the fail-closed flagged-draft approve behavior above (closes the same class of approve-bypass T-32 fixed on Mission Control, here on the Shift's Work-phase card); (3) offline optimistic stack advance — actioning a card while offline now advances the local stack immediately (optimistic UI) and reconciles against the server once connectivity returns, rather than leaving the rep staring at a stale card with no feedback.
- Additive-only (no existing route or model modified); routes construct services lazily in-handler (same build-safe convention as the rest of the codebase). 1330 tests post-merge (net +92 over T-32's 1238 — T-34's own ratios/learning-state/shift-service/shift-route/shift-UI coverage; T-34 and T-32 shared no touched file but `prisma/schema.prisma`, which merged cleanly with both units' models intact, so nothing was lost on either side).

## [2.0.0-build.T32] — BUILD PHASE — 2026-07-18
### T-32 — WP04 Mission Control 6-zone Today + Grove organism + independent-zone-failure + legacy-demo retirement (re-QC 9.4, zero critical)
- New `/today` Mission Control surface (master-spec §9.5, uiux §5.2): `GET /api/mission-control/today` (`withOnboardingGate`-gated) aggregates six independently-sourced zones — Anchor Header (greeting), Briefing, Action Queue, Pipeline Glance, Ratio Cards, Calendar Strip — each rendered by its own `zones/*.ts` builder against its own Prisma reads, with no query shared across zones.
- Independent-zone-failure guarantee (uiux AC-5.2-6): `buildMissionControlToday`'s `safeZone` wraps EACH of the six zone builders in its own try/catch, turning a failing zone into that zone's own `{ status: 'error' }` result — it can never reject the aggregator's `Promise.all` or touch a sibling zone's data. `ZoneErrorBoundary.tsx` renders the same isolation client-side (a thrown render error in one zone's React tree degrades only that zone). Proven with teeth in `tests/unit/mission-control-today-service.test.ts`: a fake DB with exactly one query throwing still returns real data for the other five zones.
- The Grove (uiux §3): an ambient living-organism visualization on 8 states (`seed`/`sprout`/`thriving`/`growing`/`quiet`/`resting`/`bloom`/`stale`) driven by three independently-derived visual channels — Branches (Grow), Leaf density/color (Engage), Fruit (Wealth) — each computed from its own Law's inputs alone (AC-3-1, no cross-term). Tokens-only rendering (T-05); the decorative `aria-hidden` SVG uses `fill-opacity` for dulled/stale dimming, never CSS `opacity` or a translucent `color:` on the adjacent, normally-announced caption text.
- Fail-closed queue approve (QC re-pass fix, closes the CFE approve-bypass): `POST /api/mission-control/queue-action` → `actOnQueueDraft` now rejects approving any `DraftMessage` whose `cfe_outcome` is not `PASS` (`reason: 'requires_review'`) — a CFE `FLAG` or `BLOCK` draft can no longer be one-tap-approved from Mission Control's Action Queue zone and is routed instead to T-33's Approval Inbox for full review. Ownership is re-checked inside `actOnQueueDraft`/`confirmAppointment` against the session user before any write, same as `queue-action`'s sibling routes.
- Retires the inert legacy demo scaffold confirmed dead by QC: `src/services/agent-layer/*`, `src/types/agent-layer.ts`, the demo `GET /api/agents` route (the real `/api/agents/dispatch` is untouched), and the demo `/api/mission-control/briefing` route are deleted outright; `tests/unit/mission-control-ui.test.ts` asserts by path that none of them exist and that no remaining source file references the retired module or `buildDemoBriefing`. `tests/unit/wp01-onboarding-gate.test.ts`'s "LIVE downstream route" gate coverage now exercises the real `/api/mission-control/today` route in place of the retired briefing demo.
- Additive-only (no schema change); routes construct services lazily in-handler, same build-safe convention as the rest of the codebase. 1238 tests post-merge (net +94 over T-33's 1144 — T-32's own zone/Grove/queue-action/legacy-demo-retirement coverage; the two units touched disjoint files so nothing was lost on either side).

## [2.0.0-build.T33] — BUILD PHASE — 2026-07-18
### T-33 — WP04 Approval Inbox (no batch-approve) + edit-re-enters-CFE + Activity Ledger + per-contact controls (QC 9.3, zero critical)
- New Approval Inbox (`/inbox`, backed by `GET /api/approval-inbox` + `approve`/`decline`/`edit` routes) surfaces T-30's `DraftMessage` queue (`cfe_outcome`/`approval_state`) for strictly per-item human sign-off: `approval-boundary.ts`'s `rejectBatchApprove` architecturally blocks any batch/array/bulk-id shape across all three mutating routes (single-id only) — there is no "approve all" path anywhere in the surface, mutation-tested by neutering the guard (8 red).
- Edit re-enters CFE (§9.3): `ApprovalInboxService.editDraft` re-evaluates the edited text through `ComplianceFilterEngine.evaluateContent` BEFORE persisting — an edited draft that now reads `blocked` (or the CFE is down/times out) is HELD, fail-closed, and can never be approved as-is. Proven via a real (not mocked) CFE call-count mutation test, not just a status-field check (removing the re-entry call itself turns 4 tests red).
- New Activity Ledger (`GET /api/activity-ledger`) is read-only — no update/delete method exists on the route — and ownership-scoped to the requesting rep's own contacts/drafts; forged `x-user-id` is inert, same as every other route in the codebase.
- New per-contact controls (`/api/contacts/controls`, `ContactControls` UI) let a rep independently pause agent activity or set do-not-contact per contact; ownership-checked before any write (dropping the ownership check turns 6 tests red across ledger + controls).
- Fixed a real pre-existing repo defect the build surfaced: `.gitignore`'s unanchored `inbox/` rule was matching as git's `**/inbox/` shorthand, silently swallowing `src/app/inbox/**` from version control. Corrected to the root-anchored `/inbox/` — `src/app/inbox` is now tracked, the unrelated root-level `inbox/` (docs, no secrets) remains ignored; verified nothing else fell in the blast radius.
- Additive-only schema (`DraftMessage.decline_reason`/`decline_note`, both nullable) — CFE and agent-runtime internals untouched, routes construct services lazily in-handler (same build-safe convention as the rest of the codebase). 1144 tests (80 new).

## [2.0.0-build.T31] — BUILD PHASE — 2026-07-18
### T-31 — WP04 per-rep cost model + kill-switch + in-roster degradation ladder (QC 9.2, zero critical)
- Real per-tier Claude pricing (§4.5) replaces T-30's placeholder `EstimatingCostModel`: `TierPricingCostModel` prices actual token usage on the ACTUAL Claude tier used (Haiku 4.5 $1.00/$5.00, Sonnet 5 $3.00/$15.00 durable rate — deliberately not the temporary $2.00/$10.00 intro rate expiring 2026-08-31 — Opus 4.8 $5.00/$25.00, all per 1M tokens), with the published 50% Batch API discount applied when `batched`. A degraded run reports the real (Haiku) tier, so it is never billed as if it ran on Sonnet.
- New multi-scope kill-switch + circuit breaker (§4.5/§4.6): additive `AgentKillSwitch` model (PLATFORM singleton / ORG / REP, unique on `scope`+`scope_id`) persists manual and auto-tripped state; `BudgetKillSwitchRunGate` — wired into T-30's existing `RunGate` seam on `AgentRuntime.runAgent`, consulted BEFORE any Claude spend — enforces per-rep daily ceilings (tier x intensity, Low < Medium < High, PAID_INDIVIDUAL > FREE), an ENTERPRISE org-aggregate ceiling, and a platform-wide daily spend circuit breaker, alerting the operator on auto-trip. Critical paths (CFE, inbound handling, appointment confirmations) bypass the gate entirely per spec; an unknown-rep lookup fails OPEN (a cost control, not an identity check) while every kill-switch/budget denial fails CLOSED (HELD, no tokens spent). New session-gated `POST`/`GET /api/agents/kill-switch` (`withRole`) lets a rep toggle only their own REP scope, upline-class roles toggle only their own ORG (ADMIN may target any org), and only ADMIN toggles PLATFORM — no route trusts a forged `x-user-id`/`x-organization-id` header.
- In-roster degradation ladder (§4.4/§4.6, closes WP04 checkpoint #14): `DegradingModelClient` wraps the injected `AgentModelClient` — Sonnet 5 under a genuine 429/529 rate-limit or overload signal (never a 4xx validation error or missing-credential failure) retries ONCE at Haiku 4.5, in-roster only; Haiku (the floor) or Opus (never a degradation target) have no lower rung, so a transient failure propagates unchanged into T-30's existing FAILED-and-rethrow idempotent-retry path. If the Haiku retry also fails, `DegradationFloorExhaustedError` holds the run (no draft, no send) rather than falling off-Claude. A proactive cost-pressure check (`isUnderCostPressure`, 80% of the rep's daily ceiling) can step non-quality-critical Sonnet drafting down to Haiku before a live 429 ever occurs. `DegradationAnnotatingStore` appends an honest "running on a lighter model" note to `reasoning_log` on every degraded run — never a silently-relabeled Sonnet result.
- Inngest production-dispatch deps wired + comment fix: `inngest-functions.ts` now builds the real cost model / kill-switch gate / degradation-ladder model client via `buildProductionAgentRuntimeDeps(userId)` (lazy per-invocation, never at module scope) and passes them into `dispatchAgentJob` at the one production call site — without this wiring the runtime would silently fall back to T-30's `AllowAllRunGate`/`EstimatingCostModel`/undegraded client defaults. Also corrects a stale comment: the job runs inside a single `step.run(...)` call (no per-step decomposition to resume from) — a retry re-executes the whole handler from the top, and it is `AgentRuntime`'s own `idempotencyKey`/`AgentRun`/`IdempotencyLog` state, not Inngest step-memoization, that makes a replay safe. 1064 tests (40 new).

## [2.0.0-build.T30] — BUILD PHASE — 2026-07-18
### T-30 — WP04 nine-agent runtime core + Inngest durable queue + runtime model map + CFE on sync path (QC 9.2, zero critical)
- New `runtime-model-map.ts` (§4.4) is the single source of truth for the nine agents (§4.2 — Prospecting, Pre-Sale Nurture, Post-Sale Nurture, Appointment Setting [sequential — the one non-parallel agent, §4.1 principle 3], Reporting, Quota, IPA Value, Inactivity/Re-engagement, Warm Market Sub-Agent) and their per-step Claude tier (`tierForStep` throws on an unmapped role rather than silently defaulting). Every id in `CLAUDE_MODEL_IDS` is a `claude-*` id (Haiku 4.5 / Sonnet 5 / Opus 4.8) — no non-Claude tier exists anywhere in the map. IPA Value's periodic self-optimization is the only Opus 4.8 workload and is explicitly `batched: true`, off the per-message path.
- `AgentRuntime` (`agent-runtime.ts`) is the single stateless executor all nine agents run through: idempotency/crash-safe resume (a replayed dispatch event no-ops via `wasProcessed`/`markProcessed`), per-contact `do_not_contact`/`agents_paused` halts before any generation, a RunGate budget/kill-switch seam (T-31 stub, `AllowAllRunGate` default), and — for content agents — an immediate CFE-unavailable fast-pause HOLD before spending any tokens.
- CFE now sits directly on the sync path (§2.3/§5), not bolted on after: every `contact_outbound`/`rep_facing` agent output is routed through `ComplianceFilterEngine.evaluateContent` before a `DraftMessage` is ever created, and the Approval-Inbox row is created ONLY after that verdict returns. `blocked` band or `verdict.held` (CFE down/timeout/exception) both fail closed to HELD (never sendable); `clear`/`flag` both still land as PENDING in the Approval Inbox pending human sign-off — a flagged draft is never auto-blocked from reaching the rep, but it carries its CFE band (§9.2). Rep-facing narrative copy (the Reporting agent's briefing) is deliberately routed through the CFE too (§5.4 — "motivational copy dressed as encouragement is still an income claim"); pure numeric/analytic output (Quota, IPA metrics) needs no CFE content decision and completes without one.
- `AnthropicRuntimeClient` (the real Claude call path) is Claude-only, fail-closed by construction (§0.3): a missing `ANTHROPIC_API_KEY` throws `MissingClaudeCredentialError` synchronously — no network attempt, no non-Claude fallback, no fabricated completion — and a defensive check refuses to place any model id on the wire that doesn't start with `claude-`. `AgentRuntime` catches that error and HOLDs the run with an honest, non-fabricating rep-facing message ("your agents are resting..."); any other (transient) model error is recorded FAILED and rethrown so Inngest's retry actually re-runs the job (not marked processed, so no false dedup).
- Durable queue (D-4, Vercel-native Inngest): `durable-queue.ts` defines the event contract and mockable `DurableQueue` producer boundary with zero dependency on the `inngest` package, so the full dispatch path is Jest-unit-testable with no live queue server (`InMemoryDurableQueue.drain` replays sent events through the same `dispatchAgentJob` handler Inngest would call). The real wiring (`inngest-functions.ts`, `src/lib/inngest/client.ts`, `POST /api/inngest`) registers the function with `retries: 4` and wraps the handler in `step.run(...)` so a mid-run crash resumes from the last completed step rather than re-executing it; run-level idempotency (not Inngest's own dedup) is what makes a replay a no-op. New session-gated `POST /api/agents/dispatch` (`withOnboardingGate` — userId comes from the verified session, a forged `x-user-id` header is inert) enqueues onto the real `InngestDurableQueue`, constructed lazily per-request (never at module scope, preserving the key-less-build convention every other route in this codebase follows).
- Closes the T-23 advisory (WP02 → WP04): the runtime constructs `SegmentationService`/`MemoryJoggerService` with `HaikuSegmentationClient`/`HaikuMemoryJoggerCategoryClient` INJECTED as the default deps, not the services' own local-heuristic fallback — forgetting this wiring would have silently downgraded segmentation/memory-jogger to regex instead of Haiku 4.5, with no error to signal it; both clients are exposed on the runtime instance so tests assert the Haiku wiring directly rather than inferring it from behavior. 1024 tests (38 new).

## [2.0.0-build.T29R2] — BUILD PHASE — 2026-07-18
### T-29R/T-29R2 — WP03 gate remediation: unlicensed-state exclusion + jurisdiction capture + needs-jurisdiction surfaced state (QC 9.1, zero critical)
- Closes the WP03 gate's blocking checkpoint-7 finding (regulated-rep contacts in an unlicensed state were being scored/tiered/queued as actionable): `eligibility.ts`'s `checkJurisdictionExclusion` now consults `LicensingService.getLicensedJurisdictions()` and mirrors the existing `do_not_contact`/minor/opt-out soft-exclusion pattern. Regulated (Primerica) reps exclude any contact whose jurisdiction isn't in the licensed set; an empty or throwing lookup fails CLOSED (one bad lookup never crashes the queue open). Universal reps are a structural no-op — `LicensingService` is never called, now proven with real jest-spy call-count teeth (not just a swallowed-throw check).
- New `Contact.jurisdiction` field (additive schema + migration) is actually captured on two production paths, closing the gap the first compliance QC pass caught (mechanism was clean but nothing wrote the field, so every regulated rep's queue was silently empty): CSV import recognizes `state`/`jurisdiction`/`contact-state`/`licensing-state` aliases (an absent column still imports, just leaves it null), and it's independently settable via the existing session-gated, ownership-checked, forged-`x-user-id`-inert `PATCH /api/contacts/flags`.
- New `ReadinessTier.NEEDS_JURISDICTION` (additive enum-only migration): a regulated rep's contact with an unknown/null jurisdiction is held out of the §8.3 action queue — can't draft compliant outreach without knowing the state — but is SURFACED in the ritual-review view as a remediable data-completion prompt, never silently dropped and never conflated with a confirmed-unlicensed `EXCLUDED` contact. Confirmed-unlicensed and empty/throwing-lookup cases remain fail-closed `EXCLUDED`, unchanged.
- Score-invisible contract re-proven despite the `readiness-engine.ts` touch required to add the new tier: `PublicQueueItem` carries no score field, `assertNoRawScoreLeak` covers the new tier on every item and the assembled payload, and the ritual UI only ever reads a label + boolean for it. Org-gate, Primerica calibration overlay, and the T-27 anti-pattern boundary are untouched; all 5 prior WP03 criticals remain proven absent. 986 tests.

## [2.0.0-build.T27] — BUILD PHASE — 2026-07-18
### T-27 — Readiness-sorted action queue + architecturally-blocked anti-patterns + notes doctrine linter (QC 8.5 base, fixes confirmed 9.4, zero critical)
- Action queue (`action-queue` + `prioritized-queue` GET routes) surfaces T-26's tier-sorted, score-invisible, excluded-never-in-queue contract to the action-complete workflow — reconfirmed by new boundary tests; no route ever emits a score-shaped field.
- New `action-boundary.ts` blocks 3 §8.5 anti-patterns architecturally, at the request boundary, fail-closed (400 `ANTI_PATTERN_BLOCKED`, never a silent warn): `rejectTierOverride` (tier/score/priority body keys), `rejectBatchPayload` (array/contactIds bulk-actioning), `rejectSortOverride` (sort/orderBy query params) — wired into `action-complete` and both queue GET routes.
- Notes doctrine linter extended to a second surface: `action-complete` lints an optional note through `lintNote` → `VocabularyClassifier`, storing the corrected text as an encrypted `ContactInteraction` of type `ACTION_NOTE`. No new routes (3 existing extended, all lazy in-handler); no schema change.
- QC fast-follow fixes (base QC 8.5 → confirmed 9.4, zero critical): (1) validate-before-commit atomicity — `lintNote` now runs BEFORE `markActionComplete` so an over-length note can no longer be rejected with 400 `NOTE_TOO_LONG` after the action already committed; (2) anti-pattern guards hardened to be case-insensitive and 1-level-nested-aware (`findForbiddenKey`/`findBatchHit`/`rejectSortOverride`), closing an aliased/nested/case-variant bypass that previously fell through to a silent 200 (the underlying invariant held structurally throughout — routes never read those fields — so this closes a contract-completeness gap, not a security breach). 951 tests.

## [2.0.0-build.T28] — BUILD PHASE — 2026-07-18
### T-28 — Warm-Market Ritual UI + plots/contact card + independent recruit/client toggle write-path (QC 9.0, zero critical)
- Ritual UI (`/ritual/warm-market`) wires the three pure layer components (Blank Canvas soft-gate / 6-cluster Qualities Flip — §8.1 reconciliation, not 5 — / Background Matching) to the real T-26 engine routes, resumable per layer (re-entry lands on the incomplete layer; a failed save keeps the layer interactive with retry, never discarding input). T-26's hidden readiness score stays invisible end-to-end — the ritual only ever reads completed-layer booleans and the post-Layer-3 queue, never a score-shaped field.
- Community plots/contact card (uiux §4.6): avatar/initials, 1-5 closeness dots, a recency indicator (icon+tooltip text, never color alone — §6.1), a segment tag, and the five card states (rest/needs-info/excluded/agents-paused/removed-from-phone) — all on the T-05 Living Field design tokens (no raw hex).
- Closes the carried-forward `is_recruit_target` / `is_client` toggle write-path gap (uiux §4.6 "two independent flag toggles"): new `ContactFlagsService.setFlags` only includes a column in its Prisma update when the caller explicitly supplied that field, so setting one flag never touches the other (structural independence, not independence-by-convention). `PATCH /api/contacts/flags` is session-gated (`withOnboardingGate`, never a forged `x-user-id`), ownership-checked (contact must belong to the session user) before any write, and constructs its service lazily in-handler (same build-safe convention as every `harvest-method/*` and `agent-queue` route — no module-scope construction to trip `next build`'s page-data collection). `ContactCard`'s two toggles call separate callbacks, never a combined handler, so the UI itself cannot violate the independence guarantee even by accident. 897 tests.

## [2.0.0-build.T26] — BUILD PHASE — 2026-07-18
### T-26 — WP03 three-layer method + hidden readiness engine (QC 9.2, 1 QC loop) — first WP03 unit
- Three-layer warm-market method (Blank Canvas soft-gate / 6-cluster Qualities Flip / Background Matching) with enforced layer order (no short-circuit — action queue unavailable until all 3 complete). Hidden 0–100 readiness score (§8.2 weights 0.30/0.25/0.20/0.15/0.10) drives priority tiers but is NEVER surfaced (assertNoRawScoreLeak throws on any score-shaped key in a public payload).
- Excluded contacts (do_not_contact / DO_NOT_CONTACT / minor / opted-out) never enter the ranking or action queue, re-evaluated live per request. Primerica calibration overlay only behind the org gate (structurally absent for universal users; assertNoPrimericaLeak). Upline visibility is aggregate-only (assertAggregateOnly). Replaced pre-rebuild baseline that had org-gating inverted (refused non-Primerica).
- Additive schema (HarvestMethodState, ContactMethodProfile + 2 enums) + migration. Routes instantiate services lazily in-handler (build-safe without the encryption key). 853 tests. Deferred/tracked: §8.3 opener-drafting + rep-opt-in consent gate for the upline aggregate surface → WP04.

## [2.0.0-build.T23] — BUILD PHASE — 2026-07-17
### T-23 — Segmentation + Memory Jogger + agent pipeline; Contact-PII decrypt reads (WP02, QC 8.75)
- Haiku-4.5 contact segmentation (DI-mockable, fail-closed on missing key, no non-Claude fallback; deterministic 0–100 score, A-list ≥70). Memory Jogger surfaces DECRYPTED contact PII (via T-22's decryptContactPII) with dedup + skip-count. Agent pipeline (contact→agent typed contract) + new session-gated /api/contacts/agent-queue route (ownership-checked, forged-header-inert).
- Fixes the T-22 ciphertext-read consequence: memory-jogger + pipeline now decrypt Contact PII; replaced 2 falsely-green plaintext-mock tests with a real encrypt→store→decrypt round-trip. Additive schema Contact.memory_jogger_skip_count + migration. 748 tests.
- Advisory to WP04 (T-30): inject HaikuSegmentationClient/HaikuMemoryJoggerCategoryClient — services default to the local heuristic; forgetting = silent regex instead of Haiku.

## [2.0.0-build.T24] — BUILD PHASE — 2026-07-17
### T-24 — Hidden Earnings engine: FTC-safe formula, org-gated Primerica calibration, safe-harbor on every render, 0-3 growth path (WP02, QC 9.8)
- FTC-safe potential-earnings formula (framed potential/illustrative, never a guarantee). Primerica calibration triple-gated behind the org gate — a non-Primerica user never receives Primerica-specific numbers (branch check + assertPrimericaGate + payload leak-scan at the wire).
- The safe-harbor line is emitted on EVERY figure-bearing path (render, screen-reader, outreach) via assertSafeHarborPresent (throws if missing/altered). 0–3 contacts — and any non-positive computed value — render a growth path, never $0/NaN/Infinity (hostile inputs sanitized). Outreach text is CFE-gated (released-only). /api/contacts/hidden-earnings is session-gated, org/count read server-side. 769 tests.

## [2.0.0-build.T-R7] — BUILD PHASE — 2026-07-18
### T-R7 — DSAR export decrypts Contact PII before serialize (WP11 remediation, QC 8.9)
- processExport decrypts every Contact's PII before serializing the GDPR/CCPA export (JSON+CSV); per-field safe degradation on decrypt failure (marked-unavailable, no crash/no ciphertext leak). Deletion/FINRA carve-out/legal-hold unchanged. 725 tests.
- Follow-up (T-R9): decrypt User.solution_number in the export + exclude password_hash/MFA material.

## [2.0.0-build.T22] — BUILD PHASE — 2026-07-17
### T-22 — The Vault: contact ingestion + encrypted PII + idempotent import (WP02, QC 8.6) — first Wave 3 unit
- Four ingestion modalities (CSV, iOS native, Android native, Google Contacts) converge on one encrypting upsert. Contact PII (first/last name, phone, email, notes) is AES-256-GCM encrypted at rest (CONTACT_ENCRYPTION_KEY, fail-closed); dedup/match via keyed HMAC (phone_hash/email_hash), never plaintext. Fixes a pre-existing plaintext-PII bug in contact.service.ts.
- Idempotent + resumable import via a new ImportBatch model (replay a completed key = no-op; infra failure resumes at the row without advancing the cursor; cross-key HMAC dedupe). Minors (under-18, fail-toward-caution) set do_not_contact + DO_NOT_CONTACT + OptOutRegistry(reason:minor) — unreachable for outreach. /api/contacts/import is session-gated (withOnboardingGate), never trusts a forged header. 724 tests.
- NOTE: Contact PII columns are now ciphertext — downstream consumers must decrypt via decryptContactPII (tracked: T-23 memory-jogger/pipeline; T-R7 data-rights DSAR export [compliance, do early]; T-R8 legacy dedup).

## [2.0.0-build.T21R] — BUILD PHASE — 2026-07-17
### T-21R — GDPR consent capture (WP01 §6.10-10 remediation, QC 8.7)
- Dedicated O-8.5 consent micro-step (explicit affirmative, default-off, Continue disabled until acted) → POST /api/onboarding/consent (session-authed) calls WP11 ConsentManager, writes a versioned+timestamped ComplianceConsent('gdpr', given:true) row and sets User.gdpr_consent=true.
- Completion precondition (role-agnostic, fail-closed): /api/onboarding/complete rejects with 400 GDPR_CONSENT_REQUIRED unless gdpr_consent===true — no role can reach GATED_COMPLETE without a recorded consent (proven across all 5 roles).
- Revoke path (DELETE /api/onboarding/consent) writes a given:false record and clears the flag. The §6.10-1 downstream route gate is untouched. 694 tests.
- Closes the WP01 wave-gate GDPR gap. Follow-up (tracked in T-20): wire the dense UPLINE/RVP/DUAL onboarding UI to the consent route + have the durable completion writer read User.gdpr_consent.

## [2.0.0-build.T20] — BUILD PHASE — 2026-07-17
### T-20 — Onboarding UI (O-1..O-9) + end-to-end §6.10-1 gate enforcement + encrypted solution-number on register (WP01, QC 9.1, 2 QC loops)
- O-1..O-9 onboarding screens + dense upline/RVP track, consuming the merged WP01 engines (identity/org-gate, Seven Whys invisible-score contract, sponsor matching/waitlist, tracks + §16.5 licensing hard-block), on the Living Field design tokens (no raw hex). Includes O-5 outreach-consent toggle (default off), O-2 photo capture (+initials fallback), DUAL persona switcher, Hidden Earnings Reveal (safe-harbor + zero-data growth path + no-share + single SR utterance).
- §6.10-1 hard gate now enforced end-to-end: middleware redirects non-GATED_COMPLETE users off all WP02–WP10 page subtrees; withOnboardingGate wraps all downstream data API routes (deny-by-default, live-DB status wins over a stale token). /api/auth/register now 7-digit-checks + AES-256-GCM-encrypts the solution number (no plaintext). Legacy onboarding service path retired. 653 tests.

## [2.0.0-build.T19] — BUILD PHASE — 2026-07-17
### T-19 — Sponsor matching, invites, access-tier assignment, downstream contracts (WP01, QC 9.0, 1 QC loop)
- Sponsor matching never dead-ends (no eligible sponsor → waitlist, not an error); invite state machine (7-day expiry, resend cap ≤3 / 24h cooldown); 9 typed §6.9 downstream contracts (projectToWP02..WP10).
- Access tier derives ONLY from §6.7 signals (auth source + sponsor/org), NEVER a commitment score — closes a payment bug where a sponsored user could be assigned a PAID tier. Live /api/onboarding/complete + /api/auth/register + the legacy determineAccessTier all route through assignAccessTierFromSignals. Tiers: FREE_ORG_LINKED/$0, FREE_PAID_EXTERNAL/$0, PAID_INDIVIDUAL/$297, ENTERPRISE/$25,000 (no $49/$199). RBAC: invite own-only for reps, tier admin-only. 571 tests.

## [2.0.0-build.T18] — BUILD PHASE — 2026-07-17
### T-18 — Seven Whys engine: Sonnet runtime, invisible >70 resonance gate as care, consent-off (WP01, QC 8.8)
- Sonnet-5 guided conversation (one question per turn, DI-mockable; missing ANTHROPIC_API_KEY throws with no non-Claude fallback).
- Hidden 0–100 resonance score gates progression at >70 but is INVISIBLE by construction (rendered-turn type has no score field; verified by key+JSON scan and mutation test). A ≤70 turn triggers a caring re-prompt, never a failure/score message.
- Anchor statement composed, doctrine-vocabulary-guarded, AES-256-GCM encrypted at rest (WHY_SESSION_ENCRYPTION_KEY, fail-closed). use_in_outreach_consent defaults false at schema/create/update; only an explicit setter flips it.
- Any anchor routed toward outreach passes through the CFE (fail-closed; consent-false short-circuits; only a released verdict allows). 508 tests.

## [2.0.0-build.T17] — BUILD PHASE — 2026-07-17
### T-17 — WP01 onboarding core: identity gate, 5 roles, org gate, solution-number, tracks A/B/D (QC earned 9.0/10, 1 QC loop) — first Wave 2 unit
- Fail-closed master identity gate (401/403, no coercion); only GATED_COMPLETE onboarding status unlocks downstream.
- Five roles + DUAL: canInPersona decomposes DUAL to the active persona's base role (no REP∪UPLINE bleed); PersonaScopedStore partitions rep- vs upline-persona data; §17.2 self-review escalation.
- Org gate locks the §17.1 branch (PRIMERICA vs universal); data-layer Primerica-leak tripwire matches raw + normalized text (catches mixed-case + camelCase field names); universal users are Primerica-free by construction.
- Solution-number: 7-digit format-check only, verified=false, masked, encrypted at rest, never logged/echoed/trusted-as-auth; a valid number cannot buy the Primerica branch (only org_type does). Legacy 6-8-digit validator retired (delegates to the authoritative 7-digit check); legacy OrgType/AccessTier enums retired to the Prisma enums.
- Tracks A/B/D state-machine shells with the T-13 licensing hard-block (only LICENSED clears; unlicensed/pre/expired route to compliance advisory). 486 tests.
- NOTE: end-to-end route-level gate enforcement (wiring these modules into WP02-WP10 routes + /api/auth/register solution-number encryption) is contracted to T-20.

## [2.0.0-build.T15] — BUILD PHASE — 2026-07-16
### T-15 — Breach notification & incident response, GDPR 72h (WP11, QC earned 9.1/10) — FINAL WP11 unit
- SecurityEvent correlation: weighted 60-min sliding-window classifier declares an incident at threshold; a lone breach_incident declares alone. Deterministic.
- GDPR Art.33 72-hour clock: starts for every breach class except (human-triaged) NOT_PERSONAL_DATA (fail-toward-caution); missing start reports maximally urgent (never "no clock"); freezes at notification; late notification recorded as late. Un-triaged breach can never be silently dropped.
- Incident runbook state machine (DETECTED→TRIAGED→CONTAINED→NOTIFIED→RESOLVED) with a hard "notify before resolve" guard for clock-applicable breaches. RBAC: incident_response is RVP/ADMIN only via the §16.6 matrix.
- Append-only IncidentEvent ledger (current state = read-time projection, no mutable status column), mirrored into T-10's hash-chained audit store. SecurityEvent bridge is a pure decorator (T-12's security-event.ts byte-unchanged). Additive schema (IncidentEvent) + migration. 423 tests.
- Completes WP11 (compliance) — all 7 units (CFE, audit, data-rights, account-security, licensing, RBAC, incident-response) now in trunk.

## [2.0.0-build.T12] — BUILD PHASE — 2026-07-16
### T-12 — Account security: MFA/TOTP, rate limiting, credential-stuffing, session-hijack defense (WP11, QC earned 9.25/10, 1 QC loop)
- Real RFC 6238 TOTP (otplib), secret AES-256-GCM at rest, fail-closed without MFA_ENCRYPTION_KEY; single-use bcrypt-hashed recovery codes.
- Step-up MFA freshness is bound to a SERVER-side single-use proof (User.mfa_stepped_up_at, atomic compare-and-swap consume) — the jwt callback ignores any client-supplied timestamp, closing a self-certification bypass. All /api/auth/mfa/* and /api/auth/session/* routes wrapped in withSessionSecurity; re-enrolling a second factor requires fresh step-up.
- Rate limiting (per account+IP, sliding window, exponential backoff) FAILS CLOSED on store error; credential-stuffing defenses (offline breached-password screen, success-only anomaly scoring, non-enumerating hashed keys, timing-equalized login).
- Session-hijack defense: device-fingerprint binding, 30-min idle + 12h absolute lifetime, security_version "sign out everywhere" revocation. SecurityEvent emitted at every decision point. Additive schema (User.security_version, User.mfa_stepped_up_at) + migrations. 364 tests. NOTE: in-memory rate/session stores must be swapped for a shared store (Redis) before multi-instance deploy — tracked T-R5.

## [2.0.0-build.T10] — BUILD PHASE — 2026-07-16
### T-10 — Immutable append-only audit store, hash-chained (WP11, QC earned 9.2/10)
- Append-only AuditRepository/AuditService — NO update/delete API on audit rows (verified codebase-wide); InMemory rows frozen; duplicate-id append rejected.
- SHA-256 hash chain + monotonic sequence: verifyChain detects any in-chain mutation (hash mismatch) or deletion (sequence gap / broken prev_hash). Tail-truncation resistance left to a future external anchoring checkpoint (getChainHead/Tail hooks provided) — documented.
- Durable sinks funnel CFE / licensing / data-rights events via their UNCHANGED interfaces (no edits to engine/licensing/data-rights). FINRA regulation tag preserved with priority so T-11's legal-hold carve-out query still matches.
- Rep-visible Activity Ledger, RBAC-scoped via the T-14 §16.6 matrix (own-scope always; cross-user via can(compliance_audit,read); downline resolver fail-closed). Additive schema (sequence/prev_hash/entry_hash + CFEOutcome.RECORDED) + migration. 273 tests.

## [2.0.0-build.T11] — BUILD PHASE — 2026-07-16
### T-11 — Data-rights: complete PII deletion sweep (WP11, QC earned 9.1/10, 3 QC loops)
- Right-to-erasure now scrubs ALL 12 user-owned PII models (User incl password_hash/image, Contact, ContactInteraction, OnboardingSession, WhySession, WarmMarketExercise, Message, DraftMessage incl cfe_classifier_data, UplineInvite recipient_email (direct + cross-user), LicensingRecord.license_number, AgentRun narrative fields, Milestone.shareable_asset_ref) — verified by an independent full-schema audit (no PII model missed).
- FINRA-tagged AuditEntry rows are never deleted (regulatory carve-out); an active LegalHold blocks all scrubbing (deletion held, nothing touched); deletion certificate honestly lists only models actually scrubbed.
- Retention schedules per §16.3 category; RBAC-gated legal-hold place/lift (ADMIN+RVP); CSV formula-injection guard on export. Integrated with the T-14 §16.6 Role. 211 tests.

## [2.0.0-build.T14] — BUILD PHASE — 2026-07-16
### T-14 — Authoritative §16.6 RBAC matrix enforcement (WP11, QC earned 9.35/10)
- src/lib/auth/rbac-matrix.ts encodes all 9 §16.6 rows as resource×action×role data; can(role,resource,action) is fail-closed (unknown resource/action/role all deny, ADMIN included — no implicit bypass).
- Downline raw-PII / conversation content is audited-only: the grant table is empty for everyone incl ADMIN; the sole path is canAccessDownlinePIIAudited(role, auditContext). Cross-org access for RVP is gated behind admin approval.
- requireCapability/withCapability extend T-04's requireRole/withRole. The older compliance ROLE_PERMISSIONS is now DERIVED from the matrix (one source of truth); data_rights:manage remains ADMIN+RVP only.
- Retired the stale 6-value compliance Role type (removed the spec-retired EXTERNAL) in favor of the Prisma Role enum. 218 tests.

## [2.0.0-build.T08] — BUILD PHASE — 2026-07-16
### T-08 — Compliance Filter Engine core, fail-closed (WP11 critical path, QC earned 9.1/10, 1 QC loop)
- 5 Haiku-4.5 classifiers (Income/Testimonial/Opportunity/Insurance/Referral) + vocabulary classifier (§0.5 forbidden terms force BLOCKED), DI-mockable (tests run with no API key).
- §5.4 risk banding (weights + regulation multipliers + thresholds verbatim from spec); removed a non-spec score-inflation hack.
- FAIL-CLOSED short-circuit: the ONLY release path is band=clear & not held; any classifier error/timeout/missing-credential/unavailable → held ("held for review"), zero send, HTTP 503 — no approve-on-error path (mutation-proven: flipping to fail-open breaks 6 tests).
- §5.5 licensing-phase hard-block: unlicensed / licensing-phase rep + ANY insurance-recommendation signal → blocked regardless of score (keyed on signal, not mere unlicensed status, so clean content is not over-blocked).
- Claude-only: missing ANTHROPIC_API_KEY throws with no network call and no fallback; out-of-[0,1] classifier confidence throws → held. evaluateContent() gate + backward-compat review() shim for WP04/05. Audit events emitted for T-10. 178 tests.

## [2.0.0-build.T13] — BUILD PHASE — 2026-07-16
### T-13 — State insurance licensing state machine (WP11, QC earned 9.2/10)
- FSM: UNLICENSED → PRE_LICENSING → LICENSED → LICENSE_EXPIRED (+ RENEW_LICENSE loop), guarded transitions reject illegal jumps with no state mutation or audit emission.
- Fail-closed capability gate: canPerformLicensedActivity/isLicensed true only for LICENSED; missing record or empty jurisdiction set = UNLICENSED (never inferred licensed). Content-gate levels per §16.5.
- Per-US-state jurisdiction; strictest state governs a multi-state rep; feeds CFE UserContext.licensed_states (WP01/WP03/WP08 consume the gate).
- Additive schema: LicensingRecord + LicensingStateEvent (+ migration). Audit sink for T-10. 34 new tests (151 total).

## [2.0.0-build.T05] — BUILD PHASE — 2026-07-15
### T-05 — Living Field design-system token layer (QC earned 9.8/10, 4 QC loops)
_Note: merged after T-06 due to four QC iterations (each caught a real, progressively subtler WCAG failure: opacity-on-text, viewport-dependent gradient text, dark-theme token composition). The branch was brought current with main (T-04+T-06) before merge._
- src/app/tokens.css: 6 LFDS hue ramps (soil/leaf/harvest/clay/wheat/grove); Golden Hour (light) + Pre-Dawn (dark) semantic tokens per uiux §1.2.4; corrected values (--soil-550 #5d6a62; --harvest-500 never text-on-light; --color-harvest-on-cream pinned harvest-700 in both themes); type scale, 8pt spacing, elevation, motion tokens.
- Pre-hydration theme switch (System / Golden Hour / Pre-Dawn) with no flash; legacy scaffold pages intentionally pinned light (dark-mode migration deferred to per-screen work).
- Accessibility gates (a11y failure = gate failure, uiux §6.1): verify:contrast (27 pairs + negative teeth), guard:no-opacity-on-text (static, per-instance exemption keys), and verify:rendered-contrast (Playwright, 2 themes x 2 viewports x 2 surfaces = 904 nodes) — wired into postbuild, npm test, and CI.
- /design-tokens showcase route. 117 tests. 4 pre-existing legacy AA issues (auth/onboarding pages, .side-link, .visual-root span) documented [WARN-EXEMPT] and carried to T-52.

## [2.0.0-build.T06] — BUILD PHASE — 2026-07-15
### T-06 — CI quality-gate workflow + deferred Vercel deploy stub (QC earned 8.9/10, 2 QC loops)
- .github/workflows/ci.yml: on push main/build/** + PR — npm ci, prisma generate, typecheck, lint, test, build (+postbuild verify:middleware/verify:api-auth), and actionlint over all workflows. A failure in any step fails the job.
- .github/workflows/deploy.yml: gate job reads VERCEL_TOKEN/ORG_ID/PROJECT_ID via env and emits `deployable`; deploy job runs `needs:[gate]` + `if: needs.gate.outputs.deployable=='true'` — skipped (not errored) until T-02 supplies the three GitHub secrets. (Fixed from an invalid job-level `secrets`-in-if guard.)
- Added .eslintrc.json (repo's first ESLint config; ~74 pre-existing violations warn-level, tracked as T-R2) + .nvmrc (20). docs/CI.md documents the workflows and the §19.4 ledgers convention.

## [2.0.0-build.T04] — BUILD PHASE — 2026-07-15
### T-04 — Auth.js (NextAuth) + five-role RBAC scaffold, MFA-capable (QC earned 8.7/10, 3 QC loops)
- NextAuth v4 + Prisma adapter + bcryptjs; typed session carries role (REP/UPLINE/RVP/ADMIN/DUAL) + org context.
- RBAC guard (requireRole/hasRole/roleSatisfies): DUAL = union of REP+UPLINE, ADMIN bypass (opt-out per call), fail-closed on null/invalid session.
- Route auth: middleware at src/middleware.ts gates /dashboard (unauth -> 307 /auth); postbuild verify-middleware + verify-api-auth guards fail the build if the gate is silently unregistered or a route serves real data off a forged header.
- MFA-capable session seams for T-12; loud production failure if NEXTAUTH_SECRET/AUTH_SECRET is unset; login timing-equalized against email enumeration.
- Live /api/session/whoami withRole call-site; adapter tables Account/Session/VerificationToken + migration. 114/114 tests. OPENAI_API_KEY removed from .env.example.

## [2.0.0-build.T03] — BUILD PHASE — 2026-07-15
### T-03 — Prisma schema evolved to master-spec §3 (QC earned 8.9/10)
- Schema evolved 18 -> 51 entities (13 enums, 38 models) incl SecurityEvent; resolved 5-role enum (REP/UPLINE/RVP/ADMIN/DUAL).
- Keyed, fail-closed HMAC-SHA256 (env CONTACT_HASH_PEPPER) for contact phone/email match hashes; unkeyed SHA-256 retired for PII.
- Postgres partial unique indexes: at most one ACTIVE Subscription and one ACTIVE Sponsorship per user.
- First committed migration (20260715120420_evolve_to_spec_v3) + migration_lock.toml.
- 12 new tests added (82/82 passing). Independently verified on a live Postgres 16 (zero drift; constraint enforcement + mutation-tested).

# The Harvest - Changelog

> Version-controlled log of all changes from project inception through PRD completion.
>
> **Note:** Initial drafts (v0.1.0-draft.N) used ollama/deepseek-v4-flash:cloud before model standardization.

---

## v0.1.0 — PRD Draft Complete (Current)

### 2026-04-26
| Field | Value |
|-------|-------|
| **Date** | 2026-04-26 |
| **Version** | v0.1.0-draft.1 |
| **Author** | Claudia (Orchestrator) |
| **Model** | ollama/deepseek-v4-flash:cloud |
| **Description** | Repository setup: initialized `prd-packages/harvest-app/` directory structure, created foundational PRD skeleton, defined core user stories and functional requirements, drafted data models and API contract outlines. |

### 2026-04-27
| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.1.0-draft.2 |
| **Author** | Claudia (Orchestrator) |
| **Model** | ollama/deepseek-v4-flash:cloud |
| **Description** | First QC pass: reviewed foundation sections against project goals, corrected inconsistencies in data model definitions, refined API specifications, expanded edge-case coverage in user stories. |

| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.1.0-draft.3 |
| **Author** | Claudia (Orchestrator) |
| **Model** | ollama/deepseek-v4-flash:cloud |
| **Description** | Expansion writing: completed all PRD sections including non-functional requirements, security model, error handling strategy, and deployment considerations. Added acceptance criteria for all user stories. |

| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.1.0-draft.4 |
| **Author** | Claudia (Orchestrator) |
| **Model** | ollama/deepseek-v4-flash:cloud |
| **Description** | Second QC pass: cross-referenced all sections for internal consistency, validated API contracts against data models, verified acceptance criteria traceability, fixed formatting and markdown lint issues. |

| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.1.0-rc.1 |
| **Author** | Claudia (Orchestrator) |
| **Model** | ollama/deepseek-v4-flash:cloud |
| **Description** | Assembly: merged all sections into final PRD document, generated table of contents, added version metadata and authorship headers, prepared package for stakeholder review. |

| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.1.0 |
| **Author** | Claudia (Orchestrator) |
| **Model** | ollama/deepseek-v4-flash:cloud |
| **Description** | Finalization: completed final review pass, signed off on PRD completeness, tagged release as v0.1.0. PRD package ready for handoff to development team. Future build phases will increment to v0.2.0+. |
| **Files updated** | harvest-prd.md, harvest-todo.md, harvest-qc-checklist.md, harvest-changelog.md, harvest-handoff.md |

---

## Legend

| Version Suffix | Meaning |
|---------------|---------|
| `-draft.N` | Work-in-progress iteration |
| `-rc.N` | Release candidate — ready for review |
| (no suffix) | Final release |

## Next Expected Version

**v0.2.0** — Development kickoff (build phase)

---

## v0.3.2 — QC Checklist Deduplication Pass

### 2026-04-27
| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.3.2 |
| **Author** | Claudia (Orchestrator) |
| **Description** | Deduplication pass on harvest-qc-checklist.md. Removed 22 shorter duplicate checkpoints from WP02 (6), WP05 (6), and WP11 (10) — kept the more detailed version of each. Removed condensed Final QC section that duplicated the detailed Final QC Deployment Configuration section. File reduced from 587 to approximately 555 lines. No content lost — only shorter versions of duplicated checkpoints removed. |
| **Files updated** | harvest-qc-checklist.md |

## v0.3.1 — Round 4 Final QC Checklist Patch

### 2026-04-27
| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.3.1 |
| **Author** | Claudia (Orchestrator) |
| **Description** | Round 4 corrections — added missing deliverable-mapped checkpoints to WP02, WP05, and WP11 in harvest-qc-checklist.md, and expanded Final QC pressure test with deployment configuration, edge cases, and automatic-fail conditions. |
| **Files updated** | harvest-qc-checklist.md |

## v0.2.0 — 9-Correction Package Complete

### 2026-04-27
| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.2.0 |
| **Author** | Claudia (Orchestrator) |
| **Description** | Applied all 9 corrections to PRD package (C1–C9). Fixed: WP01 dependency gating (C4), duplicate section headers (C9), WP11 critical spine position (C2), supervision infrastructure insertion (C3), parallel-run indicators (C7), WP01 data schema section (C1), foundation section numbering fix (C5), CFE Agent Integration Contract expansion (C6), QC checklist expansion with Pressure Test Protocol and scoring examples (C8). Additional fix: Section 1.8 critical spine ordering corrected. Interim re-grade: all 8 pressure test questions passed (5/8 PASS, 2/8 PASS with minor notes, 1/8 PASS with clarifying note). Package zipped and delivered to Telegram. 6 of 11 WP specs complete (WP01, WP02, WP04, WP05, WP07, WP11). |
| **Files updated** | harvest-prd.md, harvest-todo.md, harvest-qc-checklist.md, harvest-changelog.md, harvest-handoff.md |

---

## v0.3.0 — Round 3 Corrections Complete

### 2026-04-27
| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.3.0 |
| **Author** | Claudia (Orchestrator) |
| **Description** | Round 3 corrections applied. Correction 1: Expanded all 11 QC blocks in `harvest-qc-checklist.md` — each WP block now has 12 checkpoints (up from 4-6), 150+ word "What Correct Completion Looks Like" descriptions, "Written Feedback to Sub-Agent (Required If Failing)" section, and "Edge Case Tests" placeholder (file grew from 375 to 505 lines). Correction 2: Reconciled timeout discrepancy between `harvest-todo.md` (1200→1800) and `harvest-handoff.md` (noted 1800 with rationale). T06 verify text corrected to match. Interim grade: 9/10. Package zipped and delivered to Telegram. |

---

## v0.4.0 — All 11 WP Specs Complete, Integration Coherence PASS

### 2026-04-27
| Field | Value |
|-------|-------|
| **Date** | 2026-04-27 |
| **Version** | v0.4.0 |
| **Author** | Claudia (Orchestrator) |
| **Model** | ollama/deepseek-v4-flash:cloud |
| **Description** | All 11 WP specs complete and integration-coherent. WP01 v5: RVP role architecture, downstream data contracts (Section 7), upline invite state machine (Section 6), calendar contract fields, access tier/intensity schema. WP10 v5: WP01 integration contract (Section 2.4), billing RBAC (Section 11), real-time entitlement gating (Section 3.2), grace period documentation, refunds/chargebacks (Section 15.2) with section renumbering 11->15. Integration coherence v2: 9.0/10 PASS -- all 4 hard conflicts and 6 minor issues resolved. WP01 role enum now includes rvp. WP03 gating standardized as Universal with Primerica Overlay. All dependency tables corrected. Orphaned behaviors assigned. |
| **Files updated** | wp01-onboarding.md, wp10-payment-subscription.md, harvest-todo.md, harvest-changelog.md, harvest-handoff.md, qc-reports/integration-coherence-v2.md |
