.DS_Store
node_modules/
.vscode/
/inbox/
wp-specs/
PRD.md
_foundation-draft.md

.worktrees/
.vercel
.next/

# TS incremental build cache — regenerates every build/typecheck, should never be tracked
*.tsbuildinfo
.env*.local

# Playwright live-e2e local artifacts (suite itself is committed)
/test-results/
/playwright-report/
blob-report/

# Local env variants created during diagnosis — never commit credentials
.env
.env.*
